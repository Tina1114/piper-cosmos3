diff --git a/cosmos_framework/data/generator/action/transforms.py b/cosmos_framework/data/generator/action/transforms.py
index 6417a6e..f80dcb5 100644
--- a/cosmos_framework/data/generator/action/transforms.py
+++ b/cosmos_framework/data/generator/action/transforms.py
@@ -604,6 +604,27 @@ class ActionTransformPipeline:
         # 1. Resize + reflection-pad spatial dimensions to the closest predefined target from ``VIDEO_RES_SIZE_INFO[resolution]``.
         data_dict = self.video_resize(data_dict, resolution)
 
+        # Condition-only VAE inference spatially transforms the sole clean
+        # frame, then restores the logical temporal length as a zero-copy view.
+        # Subsequent prompt metadata and sequence planning therefore observe
+        # exactly the same [C,T,H,W] shape as the standard full-video path.
+        temporal_expand = data_dict.pop("inference_temporal_expand_after_resize", None)
+        if temporal_expand is not None:
+            temporal_expand = int(temporal_expand)
+            video = data_dict.get("video")
+            if not isinstance(video, torch.Tensor) or video.ndim != 4 or video.shape[1] != 1:
+                shape = None if not isinstance(video, torch.Tensor) else tuple(video.shape)
+                raise ValueError(
+                    "inference_temporal_expand_after_resize requires video shape [C,1,H,W], "
+                    f"got {shape}"
+                )
+            if temporal_expand < 1:
+                raise ValueError(
+                    "inference_temporal_expand_after_resize must be positive, "
+                    f"got {temporal_expand}"
+                )
+            data_dict["video"] = video.expand(-1, temporal_expand, -1, -1)
+
         # 2. Format the caption as structured JSON when requested; otherwise run the legacy string appenders.
         if self.prompt_json_formatter is not None:
             data_dict = self.prompt_json_formatter(data_dict)
diff --git a/cosmos_framework/data/generator/action/transforms_test.py b/cosmos_framework/data/generator/action/transforms_test.py
index 93b62f0..02dd4e3 100644
--- a/cosmos_framework/data/generator/action/transforms_test.py
+++ b/cosmos_framework/data/generator/action/transforms_test.py
@@ -82,6 +82,33 @@ def test_video_padding_round_trips_to_unpadded_region() -> None:
     torch.testing.assert_close(round_tripped, video)
 
 
+@pytest.mark.L0
+def test_condition_only_temporal_expand_matches_full_video_transform() -> None:
+    pipeline = ActionTransformPipeline(tokenizer_config=None, max_action_dim=4)
+    frame = torch.arange(3 * 4 * 5, dtype=torch.uint8).reshape(3, 1, 4, 5)
+    action = torch.zeros(5, 2)
+    common = {
+        "ai_caption": "Move the arm.",
+        "action": action,
+        "conditioning_fps": torch.tensor(5),
+        "mode": "policy",
+        "domain_id": torch.tensor(0),
+        "viewpoint": "concat_view",
+    }
+
+    fast = pipeline(
+        {**common, "video": frame.clone(), "inference_temporal_expand_after_resize": 6},
+        resolution="256",
+    )
+    standard = pipeline({**common, "video": frame.repeat(1, 6, 1, 1)}, resolution="256")
+
+    assert fast["video"].shape == standard["video"].shape
+    assert fast["ai_caption"] == standard["ai_caption"]
+    assert fast["sequence_plan"].as_dict() == standard["sequence_plan"].as_dict()
+    torch.testing.assert_close(fast["video"], standard["video"])
+    assert "inference_temporal_expand_after_resize" not in fast
+
+
 @pytest.mark.L0
 def test_action_prompt_json_formatter_drops_empty_fields() -> None:
     formatter = ActionPromptJsonFormatter()
diff --git a/cosmos_framework/model/generator/mot/attention.py b/cosmos_framework/model/generator/mot/attention.py
index f0d8766..d91566a 100644
--- a/cosmos_framework/model/generator/mot/attention.py
+++ b/cosmos_framework/model/generator/mot/attention.py
@@ -267,6 +267,190 @@ def three_way_attention(
     return out_all
 
 
+def _get_cached_und_kv(memory_value: MemoryValue) -> tuple[torch.Tensor, torch.Tensor]:
+    und_k = getattr(memory_value, "und_k", None)
+    und_v = getattr(memory_value, "und_v", None)
+    if und_k is None or und_v is None:
+        raise ValueError("Diffusion KV cache is not populated for gen-only attention.")
+    if und_k.ndim == 4:
+        assert und_k.shape[0] == 1, f"Expected cached und_k batch dim 1, got {tuple(und_k.shape)}"
+        und_k = und_k.squeeze(0)
+    if und_v.ndim == 4:
+        assert und_v.shape[0] == 1, f"Expected cached und_v batch dim 1, got {tuple(und_v.shape)}"
+        und_v = und_v.squeeze(0)
+    assert und_k.ndim == 3 and und_v.ndim == 3, (
+        f"Expected cached und K/V to be [N,H,D], got und_k={tuple(und_k.shape)}, und_v={tuple(und_v.shape)}"
+    )
+    return und_k, und_v
+
+
+def two_way_attention_with_memory(
+    packed_query_states: SequencePack,
+    packed_key_states: SequencePack,
+    packed_value_states: SequencePack,
+    memory_value: MemoryValue,
+    packed_key_states_normalized: SequencePack | None = None,
+) -> SequencePack:
+    """Gen-only two-way attention using cached understanding K/V."""
+
+    cached_und_k, cached_und_v = _get_cached_und_kv(memory_value)
+    full_q, full_q_offsets = get_full_only_seq(packed_query_states)
+    full_k, _ = get_full_only_seq(packed_key_states)
+    full_v, _ = get_full_only_seq(packed_value_states)
+    cached_und_k_offsets = packed_query_states["_causal_seq_offsets"]
+    sample_offsets = packed_query_states["sample_offsets"]
+
+    if packed_key_states_normalized is not None:
+        # The diffusion cache stores the und K form needed by gen cross-attn.
+        # The gen K portion still comes from the current step.
+        _, full_k_offsets = get_full_only_seq(packed_key_states_normalized)
+        del full_k_offsets
+
+    packed_key_for_gen = from_mode_splits(cached_und_k, full_k, packed_key_states)
+    packed_value_for_gen = from_mode_splits(cached_und_v, full_v, packed_value_states)
+
+    num_samples = sample_offsets.shape[0] - 1
+    use_dense = num_samples == 1 and not torch.is_grad_enabled()
+    full_varlen_kwargs = (
+        {}
+        if use_dense
+        else dict(
+            cumulative_seqlen_Q=full_q_offsets,
+            cumulative_seqlen_KV=sample_offsets,
+            max_seqlen_Q=packed_query_states["max_full_len"],
+            max_seqlen_KV=packed_query_states["max_sample_len"],
+        )
+    )
+
+    full_res = attention(
+        full_q.unsqueeze(0),
+        get_all_seq(packed_key_for_gen).unsqueeze(0),
+        get_all_seq(packed_value_for_gen).unsqueeze(0),
+        **full_varlen_kwargs,
+    )
+    full_out = full_res.squeeze(0).flatten(-2, -1)
+    causal_out = full_out.new_empty((0, full_out.shape[-1]))
+    return from_mode_splits(causal_out, full_out, packed_query_states)
+
+
+def three_way_attention_with_memory(
+    packed_query_states: SequencePack,
+    packed_key_states: SequencePack,
+    packed_value_states: SequencePack,
+    attention_meta: SplitInfo | None,
+    memory_value: MemoryValue,
+    natten_metadata: dict | None = None,
+) -> SequencePack:
+    """Gen-only three-way attention using cached understanding K/V."""
+
+    cached_und_k, cached_und_v = _get_cached_und_kv(memory_value)
+    full_q, full_q_offsets = get_full_only_seq(packed_query_states)
+    full_k, full_k_offsets = get_full_only_seq(packed_key_states)
+    full_v, _ = get_full_only_seq(packed_value_states)
+    cached_und_k_offsets = packed_query_states["_causal_seq_offsets"]
+
+    if attention_meta is not None and attention_meta.null_action_supertokens:
+        full_v = full_v.clone()
+        starts = full_q_offsets[:-1].long()
+        null_positions = (
+            starts.unsqueeze(1) + torch.arange(attention_meta.num_action_tokens_per_supertoken, device=starts.device)
+        ).reshape(-1)
+        full_v[null_positions] = 0
+
+    if natten_metadata is None:
+        full_sa, full_sa_lse = attention(
+            full_q.unsqueeze(0),
+            full_k.unsqueeze(0),
+            full_v.unsqueeze(0),
+            cumulative_seqlen_Q=full_q_offsets,
+            cumulative_seqlen_KV=full_k_offsets,
+            max_seqlen_Q=packed_query_states["max_full_len"],
+            max_seqlen_KV=packed_query_states["max_full_len"],
+            return_lse=True,
+        )
+    else:
+        full_sa, full_sa_lse = multi_dimensional_attention_varlen(
+            full_q.unsqueeze(0),
+            full_k.unsqueeze(0),
+            full_v.unsqueeze(0),
+            metadata=natten_metadata,
+            return_lse=True,
+        )
+
+    full_ca, full_ca_lse = attention(
+        full_q.unsqueeze(0),
+        cached_und_k.unsqueeze(0),
+        cached_und_v.unsqueeze(0),
+        cumulative_seqlen_Q=full_q_offsets,
+        cumulative_seqlen_KV=cached_und_k_offsets,
+        max_seqlen_Q=packed_query_states["max_full_len"],
+        max_seqlen_KV=packed_query_states["max_causal_len"],
+        return_lse=True,
+    )
+
+    assert full_sa.shape == full_ca.shape
+    full_res, _ = merge_attentions(
+        outputs=[full_sa, full_ca], lse_tensors=[full_sa_lse, full_ca_lse], torch_compile=False
+    )
+    full_out = full_res.squeeze(0).flatten(-2, -1)
+    causal_out = full_out.new_empty((0, full_out.shape[-1]))
+    return from_mode_splits(causal_out, full_out, packed_query_states)
+
+
+def dispatch_attention_with_memory(
+    packed_query_states: SequencePack,
+    packed_key_states: SequencePack,
+    packed_value_states: SequencePack,
+    attention_mask: SplitInfo,
+    natten_metadata: dict | None = None,
+    memory_value: MemoryValue | None = None,
+    packed_key_states_normalized: SequencePack | None = None,
+) -> tuple[SequencePack, KVToStore | None]:
+    """Dispatch attention with diffusion KV-cache support.
+
+    A memory value with empty K/V is a prefill sentinel: run the normal path so
+    the caller can record K/V. A populated memory value is the gen-only path.
+    """
+
+    if (
+        memory_value is None
+        or getattr(memory_value, "und_k", None) is None
+        or getattr(memory_value, "und_v", None) is None
+    ):
+        return dispatch_attention(
+            packed_query_states,
+            packed_key_states,
+            packed_value_states,
+            attention_mask,
+            natten_metadata=natten_metadata,
+            memory_value=None,
+            packed_key_states_normalized=packed_key_states_normalized,
+        )
+
+    if isinstance(attention_mask, SplitInfo) and attention_mask.control_stream_token_ranges is not None:
+        raise NotImplementedError("Diffusion KV-cache attention does not support multi-control transfer yet.")
+    if isinstance(attention_mask, SplitInfo) and attention_mask.is_three_way:
+        output = three_way_attention_with_memory(
+            packed_query_states,
+            packed_key_states,
+            packed_value_states,
+            attention_meta=attention_mask,
+            memory_value=memory_value,
+            natten_metadata=natten_metadata,
+        )
+    elif isinstance(attention_mask, SplitInfo):
+        output = two_way_attention_with_memory(
+            packed_query_states,
+            packed_key_states,
+            packed_value_states,
+            memory_value=memory_value,
+            packed_key_states_normalized=packed_key_states_normalized,
+        )
+    else:
+        raise TypeError(f"Unsupported attention metadata: {type(attention_mask)}")
+    return output, None
+
+
 def multi_control_two_way_attention(
     packed_query_states: SequencePack,
     packed_key_states: SequencePack,
diff --git a/cosmos_framework/model/generator/mot/attention_test.py b/cosmos_framework/model/generator/mot/attention_test.py
index 77c3219..d126bd7 100644
--- a/cosmos_framework/model/generator/mot/attention_test.py
+++ b/cosmos_framework/model/generator/mot/attention_test.py
@@ -8,11 +8,8 @@ import pytest
 import torch
 
 import cosmos_framework.model.generator.mot.attention as attention
-from cosmos_framework.model.attention.natten import NATTEN_SUPPORTED
-from cosmos_framework.model.generator.mot.attention import (
-    build_packed_sequence,
-)
 from cosmos_framework.data.generator.sequence_packing.runtime import (
+    from_mode_splits,
     get_all_seq,
     get_gen_seq,
     get_und_seq,
@@ -20,6 +17,11 @@ from cosmos_framework.data.generator.sequence_packing.runtime import (
     set_und_seq,
     zeros_like,
 )
+from cosmos_framework.model.attention.natten import NATTEN_SUPPORTED
+from cosmos_framework.model.generator.mot.attention import (
+    build_packed_sequence,
+)
+from cosmos_framework.model.generator.utils.memory import DiffusionKVCacheMemoryValue
 
 MAX_SEQ_LEN = 24
 SEQS_PER_BATCH = 4
@@ -357,6 +359,73 @@ def test_build_packed_sequence_rejects_flex():
         )
 
 
+@pytest.mark.L0
+@pytest.mark.skipif(not torch.cuda.is_available() or not NATTEN_SUPPORTED, reason="CUDA attention backend required.")
+@pytest.mark.parametrize("impl", ["two_way", "three_way"])
+def test_dispatch_attention_with_diffusion_kv_cache_matches_standard_gen_path(impl: str):
+    device = torch.device("cuda")
+    dtype = torch.bfloat16
+    num_heads = 4
+    head_dim = 64
+
+    def make_pack(x):
+        input_pack, attention_meta, _ = build_packed_sequence(
+            impl,
+            packed_sequence=x,
+            attn_modes=["causal", "full"],
+            split_lens=[3, 4],
+            sample_lens=[7],
+            packed_und_token_indexes=torch.tensor([0, 1, 2], device=device, dtype=torch.long),
+            packed_gen_token_indexes=torch.tensor([3, 4, 5, 6], device=device, dtype=torch.long),
+            num_heads=num_heads,
+            head_dim=head_dim,
+            num_layers=1,
+            token_shapes=[(1, 4)],
+        )
+        return input_pack, attention_meta
+
+    torch.manual_seed(1234)
+    query_pack, attention_meta = make_pack(torch.randn(7, num_heads, head_dim, device=device, dtype=dtype))
+    key_pack, _ = make_pack(torch.randn(7, num_heads, head_dim, device=device, dtype=dtype))
+    value_pack, _ = make_pack(torch.randn(7, num_heads, head_dim, device=device, dtype=dtype))
+
+    with torch.no_grad():
+        standard_out, _ = attention.dispatch_attention(
+            query_pack,
+            key_pack,
+            value_pack,
+            attention_meta,
+            natten_metadata=None,
+        )
+        prefill_out, _ = attention.dispatch_attention_with_memory(
+            query_pack,
+            key_pack,
+            value_pack,
+            attention_meta,
+            natten_metadata=None,
+            memory_value=DiffusionKVCacheMemoryValue(),
+        )
+
+        query_gen_pack = from_mode_splits(get_und_seq(query_pack)[:0], get_gen_seq(query_pack), query_pack)
+        key_gen_pack = from_mode_splits(get_und_seq(key_pack)[:0], get_gen_seq(key_pack), key_pack)
+        value_gen_pack = from_mode_splits(get_und_seq(value_pack)[:0], get_gen_seq(value_pack), value_pack)
+        gen_only_out, _ = attention.dispatch_attention_with_memory(
+            query_gen_pack,
+            key_gen_pack,
+            value_gen_pack,
+            attention_meta,
+            natten_metadata=None,
+            memory_value=DiffusionKVCacheMemoryValue(
+                und_k=get_und_seq(key_pack).unsqueeze(0),
+                und_v=get_und_seq(value_pack).unsqueeze(0),
+            ),
+        )
+
+    torch.testing.assert_close(get_gen_seq(prefill_out), get_gen_seq(standard_out), atol=0, rtol=0)
+    torch.testing.assert_close(get_gen_seq(gen_only_out), get_gen_seq(standard_out), atol=1e-2, rtol=1e-2)
+    assert get_und_seq(gen_only_out).shape == (0, num_heads * head_dim)
+
+
 @pytest.mark.L0
 def test_decoder_layer_optimized_path_empty_und_tensor_shape():
     """Empty und tensors in the optimized AR path must be 2D, not 1D.
diff --git a/cosmos_framework/model/generator/mot/cosmos3_vfm_network.py b/cosmos_framework/model/generator/mot/cosmos3_vfm_network.py
index 3924576..16c020d 100644
--- a/cosmos_framework/model/generator/mot/cosmos3_vfm_network.py
+++ b/cosmos_framework/model/generator/mot/cosmos3_vfm_network.py
@@ -2,6 +2,7 @@
 # SPDX-License-Identifier: OpenMDW-1.1
 
 import math
+import os
 from typing import List, Tuple
 
 import torch
@@ -9,6 +10,9 @@ from torch import nn
 from transformers.configuration_utils import PretrainedConfig
 from transformers.modeling_utils import PreTrainedModel
 
+from cosmos_framework.data.generator.sequence_packing import ModalityData, PackedSequence
+from cosmos_framework.data.generator.sequence_packing.natten import verify_natten_parameter_list
+from cosmos_framework.data.generator.sequence_packing.runtime import get_und_seq
 from cosmos_framework.model.generator.mot.attention import SplitInfo, build_packed_sequence
 from cosmos_framework.model.generator.mot.context_parallel_utils import (
     get_context_parallel_last_hidden_state,
@@ -17,8 +21,6 @@ from cosmos_framework.model.generator.mot.context_parallel_utils import (
 from cosmos_framework.model.generator.mot.domain_aware_linear import DomainAwareLinear
 from cosmos_framework.model.generator.mot.modeling_utils import TimestepEmbedder
 from cosmos_framework.model.generator.utils.memory import MemoryState
-from cosmos_framework.data.generator.sequence_packing import ModalityData, PackedSequence
-from cosmos_framework.data.generator.sequence_packing.natten import verify_natten_parameter_list
 
 
 class Cosmos3VFMNetworkConfig(PretrainedConfig):
@@ -883,6 +885,7 @@ class Cosmos3VFMNetwork(PreTrainedModel):
         self,
         packed_seq: PackedSequence,
         memory: MemoryState | None = None,
+        und_only: bool = False,
     ) -> dict:
         """
         Forward pass for Cosmos3VFMNetwork.
@@ -893,6 +896,7 @@ class Cosmos3VFMNetwork(PreTrainedModel):
             memory: Optional MemoryState for persistent KV-cache memory
                 (AR inference or rolling-KV-cache training).  Built by
                 ``OmniMoTModel.build_memory_state()``.
+            und_only: Run only the text/reasoner pathway to populate diffusion K/V.
 
         Returns:
             dict with keys:
@@ -907,19 +911,33 @@ class Cosmos3VFMNetwork(PreTrainedModel):
         # This is intentional for proper batch norm / dropout behavior
         # assert self.training, "Cosmos3VFMNetwork only supports training mode"
 
-        packed_sequence, target_dtype = self._encode_text(packed_seq)  # packed_sequence: [N_total,hidden_size]
+        reasoner_is_offloaded = (
+            memory is not None
+            and memory.is_gen_only()
+            and os.environ.get("COSMOS3_REASONER_OFFLOAD", "").strip().lower() in {"1", "true", "yes", "on"}
+        )
+        if reasoner_is_offloaded:
+            # The decoder ignores the und input after prefill and reads its
+            # per-layer und K/V from ``memory``.  Avoid touching the offloaded
+            # token embedding here; retain the original packed metadata and
+            # allocate only the generator input buffer on the generator device.
+            gen_anchor = self.language_model.model.norm_moe_gen.weight
+            packed_sequence = gen_anchor.new_zeros((packed_seq.sequence_length, self.hidden_size))
+            target_dtype = gen_anchor.dtype
+        else:
+            packed_sequence, target_dtype = self._encode_text(packed_seq)  # packed_sequence: [N_total,hidden_size]
 
         # encode vision tokens
         original_latent_shapes: List[Tuple[int, int, int]] | None = None
-        if self.config.vision_gen:
+        if self.config.vision_gen and not und_only:
             original_latent_shapes = self._encode_vision(packed_seq, packed_sequence, target_dtype)
 
         # encode action tokens
-        if self.config.action_gen:
+        if self.config.action_gen and not und_only:
             self._encode_action(packed_seq, packed_sequence, target_dtype)
 
         # encode sound tokens
-        if self.config.sound_gen:
+        if self.config.sound_gen and not und_only:
             self._encode_sound(packed_seq, packed_sequence, target_dtype)
 
         assert packed_seq.attn_modes is not None
@@ -1059,7 +1077,14 @@ class Cosmos3VFMNetwork(PreTrainedModel):
             position_ids=packed_position_ids,
             natten_metadata_list=natten_metadata_list,
             memory=memory,
+            und_only=und_only,
         )
+        if und_only:
+            output_dict = {"last_hidden_state": get_und_seq(packed_outputs)}
+            for lbl_metadata_key, lbl_metadata_value in lbl_metadata.items():
+                output_dict[f"lbl_metadata_{lbl_metadata_key}"] = lbl_metadata_value
+            return output_dict
+
         last_hidden_state = get_context_parallel_last_hidden_state(
             packed_outputs=packed_outputs,
             parallel_dims=sequence_shard_parallel_dims,
diff --git a/cosmos_framework/model/generator/mot/diffusion_kv_cache_test.py b/cosmos_framework/model/generator/mot/diffusion_kv_cache_test.py
new file mode 100644
index 0000000..8848e12
--- /dev/null
+++ b/cosmos_framework/model/generator/mot/diffusion_kv_cache_test.py
@@ -0,0 +1,359 @@
+# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
+# SPDX-License-Identifier: OpenMDW-1.1
+
+"""Regression tests for diffusion-step understanding KV reuse."""
+
+import collections
+import inspect
+
+import pytest
+import torch
+
+from cosmos_framework.data.generator.sequence_packing.runtime import get_gen_seq
+from cosmos_framework.model.attention.natten import NATTEN_SUPPORTED
+from cosmos_framework.model.generator.mot.attention import build_packed_sequence, dispatch_attention_with_memory
+from cosmos_framework.model.generator.mot.unified_mot import (
+    Nemotron3DenseVLTextForCausalLM,
+    PackedAttentionMoT,
+    Qwen3VLMoeTextForCausalLM,
+    Qwen3VLTextForCausalLM,
+    Qwen3VLTextModel,
+)
+from cosmos_framework.model.generator.omni_mot_model import OmniMoTModel
+from cosmos_framework.model.generator.reasoner.qwen3_vl.configuration_qwen3_vl import Qwen3VLTextConfig
+from cosmos_framework.model.generator.utils.memory import DiffusionKVCacheMemoryState
+
+
+def _tiny_text_model(device: torch.device) -> Qwen3VLTextModel:
+    config = Qwen3VLTextConfig(
+        vocab_size=64,
+        hidden_size=256,
+        intermediate_size=512,
+        num_hidden_layers=3,
+        num_attention_heads=4,
+        num_key_value_heads=2,
+        head_dim=64,
+        max_position_embeddings=64,
+        attention_bias=False,
+    )
+    model = Qwen3VLTextModel(
+        config,
+        qk_norm_for_text=True,
+        qk_norm_for_diffusion=True,
+        use_und_k_norm_for_gen=False,
+    ).to(device=device)
+    # RoPE inv_freq is required to stay fp32; production materialization also
+    # keeps this buffer fp32 while the learned weights use bf16.
+    for parameter in model.parameters():
+        parameter.data = parameter.data.to(dtype=torch.bfloat16)
+    model.eval()
+    for module in model.modules():
+        if isinstance(module, PackedAttentionMoT):
+            module.dispatch_attention_fn = dispatch_attention_with_memory
+    return model
+
+
+def _make_pack(hidden: torch.Tensor):
+    device = hidden.device
+    pack, attention_meta, natten_metadata = build_packed_sequence(
+        "two_way",
+        packed_sequence=hidden,
+        attn_modes=["causal", "full"],
+        split_lens=[3, 5],
+        sample_lens=[8],
+        packed_und_token_indexes=torch.tensor([0, 1, 2], device=device, dtype=torch.long),
+        packed_gen_token_indexes=torch.tensor([3, 4, 5, 6, 7], device=device, dtype=torch.long),
+        num_heads=4,
+        head_dim=64,
+        num_layers=3,
+    )
+    return pack, attention_meta, natten_metadata
+
+
+@pytest.mark.L0
+@pytest.mark.skipif(not torch.cuda.is_available() or not NATTEN_SUPPORTED, reason="CUDA attention backend required.")
+def test_multilayer_diffusion_kv_cache_matches_standard_next_step(monkeypatch: pytest.MonkeyPatch):
+    device = torch.device("cuda")
+    torch.manual_seed(1234)
+    model = _tiny_text_model(device)
+
+    und = torch.randn(3, 256, device=device, dtype=torch.bfloat16)
+    gen_prefill = torch.randn(5, 256, device=device, dtype=torch.bfloat16)
+    gen_next = torch.randn(5, 256, device=device, dtype=torch.bfloat16)
+    gen_third = torch.randn(5, 256, device=device, dtype=torch.bfloat16)
+    prefill_pack, attention_meta, natten_metadata = _make_pack(torch.cat([und, gen_prefill], dim=0))
+    next_pack, _, _ = _make_pack(torch.cat([und, gen_next], dim=0))
+    third_pack, _, _ = _make_pack(torch.cat([und, gen_third], dim=0))
+    position_ids = torch.arange(8, device=device, dtype=torch.long)
+    joint_memory = DiffusionKVCacheMemoryState(num_layers=3)
+    split_memory = DiffusionKVCacheMemoryState(num_layers=3)
+
+    with torch.no_grad():
+        joint_prefill, _ = model(
+            prefill_pack,
+            attention_meta,
+            position_ids,
+            natten_metadata_list=natten_metadata,
+            memory=joint_memory,
+        )
+        standard, _ = model(
+            next_pack,
+            attention_meta,
+            position_ids,
+            natten_metadata_list=natten_metadata,
+            memory=None,
+        )
+        standard_third, _ = model(
+            third_pack,
+            attention_meta,
+            position_ids,
+            natten_metadata_list=natten_metadata,
+            memory=None,
+        )
+
+        # Production split prefill starts with both fused layer pathways on
+        # CPU, then streams only tensors without the ``_moe_gen`` suffix.
+        monkeypatch.setenv("COSMOS3_REASONER_OFFLOAD", "1")
+        for layer in model.layers:
+            layer.cpu()
+        und_prefill, _ = model(
+            prefill_pack,
+            attention_meta,
+            position_ids,
+            natten_metadata_list=natten_metadata,
+            memory=split_memory,
+            und_only=True,
+        )
+        assert get_gen_seq(und_prefill).numel() == 0
+        for layer in model.layers:
+            for name, parameter in layer.named_parameters():
+                assert parameter.device.type == "cpu", name
+
+        # The phase boundary loads GEN exactly once; UND stays on CPU from
+        # step 0 onward.
+        for layer in model.layers:
+            for name, parameter in layer.named_parameters():
+                if "_moe_gen" in name:
+                    parameter.data = parameter.data.cuda()
+        model.norm.cpu()
+        model.embed_tokens.cpu()
+        split_step0, _ = model(
+            prefill_pack,
+            attention_meta,
+            position_ids,
+            natten_metadata_list=natten_metadata,
+            memory=split_memory,
+        )
+        cached, _ = model(
+            next_pack,
+            attention_meta,
+            position_ids,
+            natten_metadata_list=natten_metadata,
+            memory=split_memory,
+        )
+        cached_third, _ = model(
+            third_pack,
+            attention_meta,
+            position_ids,
+            natten_metadata_list=natten_metadata,
+            memory=split_memory,
+        )
+
+        # A later policy chunk keeps GEN resident on CUDA while streaming the
+        # same UND parameters again.  The new cache must match and prefill must
+        # not move any generator tensor.
+        next_chunk_memory = DiffusionKVCacheMemoryState(num_layers=3)
+        next_chunk_prefill, _ = model(
+            next_pack,
+            attention_meta,
+            position_ids,
+            natten_metadata_list=natten_metadata,
+            memory=next_chunk_memory,
+            und_only=True,
+        )
+        assert get_gen_seq(next_chunk_prefill).numel() == 0
+        for layer in model.layers:
+            for name, parameter in layer.named_parameters():
+                expected_device = "cuda" if "_moe_gen" in name else "cpu"
+                assert parameter.device.type == expected_device, name
+
+    torch.testing.assert_close(get_gen_seq(split_step0), get_gen_seq(joint_prefill), atol=2e-2, rtol=2e-2)
+    torch.testing.assert_close(get_gen_seq(cached), get_gen_seq(standard), atol=2e-2, rtol=2e-2)
+    torch.testing.assert_close(get_gen_seq(cached_third), get_gen_seq(standard_third), atol=2e-2, rtol=2e-2)
+    joint_k = [value for value in joint_memory._und_k if value is not None]
+    split_k = [value for value in split_memory._und_k if value is not None]
+    assert len(joint_k) == len(split_k) == 3
+    for joint_value, split_value in zip(joint_k, split_k):
+        torch.testing.assert_close(split_value, joint_value, atol=2e-2, rtol=2e-2)
+    next_chunk_k = [value for value in next_chunk_memory._und_k if value is not None]
+    assert len(next_chunk_k) == 3
+    for joint_value, next_chunk_value in zip(joint_k, next_chunk_k):
+        torch.testing.assert_close(next_chunk_value, joint_value, atol=2e-2, rtol=2e-2)
+
+
+@pytest.mark.L0
+def test_reasoner_generator_parameter_placement_classification():
+    classify = OmniMoTModel._reasoner_generator_parameter_target
+    assert classify("language_model.model.layers.0.self_attn.q_proj.weight") == "cpu"
+    assert classify("language_model.model.layers.0.self_attn.q_proj_moe_gen.weight") == "cuda"
+    assert classify("language_model.model.layers.0.mlp.down_proj.weight") == "cpu"
+    assert classify("language_model.model.layers.0.mlp_moe_gen.down_proj.weight") == "cuda"
+    assert classify("language_model.model.embed_tokens.weight") == "cpu"
+    assert classify("language_model.model.norm.weight") == "cpu"
+    # The final gen norm is a tiny bridge required by the full prefill pass;
+    # unlike the large per-layer generator tower, it stays resident.
+    assert classify("language_model.model.norm_moe_gen.weight") is None
+    assert classify("language_model.lm_head.weight") == "cpu"
+    assert classify("language_model.model.rotary_emb.inv_freq") is None
+    assert classify("action2llm.linears.0.weight") is None
+
+
+@pytest.mark.L0
+def test_fixed_instruction_memory_cache_reuses_only_exact_token_identity(monkeypatch: pytest.MonkeyPatch):
+    model = OmniMoTModel.__new__(OmniMoTModel)
+    torch.nn.Module.__init__(model)
+    model.precision = torch.bfloat16
+    model.tensor_kwargs = {"device": torch.device("cpu"), "dtype": torch.bfloat16}
+    model._inference_reasoner_memory_cache = collections.OrderedDict()
+    monkeypatch.setenv("COSMOS3_REASONER_KVCACHE", "1")
+    monkeypatch.setattr(model, "_num_decoder_layers", lambda net=None: 2)
+    batch = {
+        "inference_instruction_cache": True,
+        "inference_instruction_cache_namespace": "checkpoint-a",
+        "inference_instruction_cache_max_entries": 4,
+    }
+
+    first, first_key = model._get_instruction_memory_state(
+        text_tokens=[[1, 2, 3]],
+        skip_text_tokens=False,
+        data_batch=batch,
+        net=None,
+    )
+    assert isinstance(first, DiffusionKVCacheMemoryState)
+    assert not first.is_gen_only()
+    first.init({}, torch.device("cpu"))
+    for layer_idx in range(2):
+        first.write_for_layer(layer_idx, tuple(torch.randn(1, 3, 2, 4) for _ in range(4)))
+    model._persist_instruction_memory(
+        cache_key=first_key,
+        memory=first,
+        text_tokens=[[1, 2, 3]],
+        skip_text_tokens=False,
+        data_batch=batch,
+        net=None,
+    )
+
+    second, second_key = model._get_instruction_memory_state(
+        text_tokens=[[1, 2, 3]],
+        skip_text_tokens=False,
+        data_batch=batch,
+        net=None,
+    )
+    changed, changed_key = model._get_instruction_memory_state(
+        text_tokens=[[1, 2, 4]],
+        skip_text_tokens=False,
+        data_batch=batch,
+        net=None,
+    )
+
+    assert second is first
+    assert second_key == first_key
+    assert changed is not first
+    assert changed_key != first_key
+    assert not changed.is_gen_only()
+
+
+@pytest.mark.L0
+@pytest.mark.skipif(not torch.cuda.is_available(), reason="CUDA is required for initial tower placement checks.")
+def test_initial_reasoner_offload_places_generator_on_cuda(monkeypatch: pytest.MonkeyPatch):
+    class TinyLayer(torch.nn.Module):
+        def __init__(self) -> None:
+            super().__init__()
+            self.q_proj = torch.nn.Linear(4, 4, bias=False)
+            self.q_proj_moe_gen = torch.nn.Linear(4, 4, bias=False)
+
+    net = torch.nn.Module()
+    net.language_model = torch.nn.Module()
+    net.language_model.model = torch.nn.Module()
+    net.language_model.model.layers = torch.nn.ModuleList([TinyLayer()])
+    net.language_model.model.embed_tokens = torch.nn.Embedding(8, 4)
+    net.language_model.model.norm = torch.nn.LayerNorm(4)
+    net.language_model.model.norm_moe_gen = torch.nn.LayerNorm(4)
+    net.language_model.lm_head = torch.nn.Linear(4, 8, bias=False)
+
+    model = OmniMoTModel.__new__(OmniMoTModel)
+    torch.nn.Module.__init__(model)
+    monkeypatch.setenv("COSMOS3_REASONER_OFFLOAD", "1")
+
+    def fail_if_storage_is_copied(*args, **kwargs):
+        raise AssertionError("allocate_empty=True must not copy cold-start tensor storage with Tensor.to()")
+
+    monkeypatch.setattr(torch.Tensor, "to", fail_if_storage_is_copied)
+    model._place_initial_offload_towers(net, allocate_empty=True, log_result=False)
+
+    for name, parameter in net.named_parameters():
+        placement = model._reasoner_generator_parameter_target(name)
+        if placement == "cuda":
+            assert parameter.device.type == "cuda", name
+        elif name.startswith("language_model.model.layers."):
+            assert parameter.device.type == "cpu", name
+        else:
+            assert parameter.device.type == "cuda", name
+
+
+@pytest.mark.L0
+def test_causal_lm_wrappers_accept_und_only():
+    for wrapper in (Qwen3VLTextForCausalLM, Qwen3VLMoeTextForCausalLM, Nemotron3DenseVLTextForCausalLM):
+        assert "und_only" in inspect.signature(wrapper.forward).parameters
+
+
+@pytest.mark.L0
+@pytest.mark.skipif(not torch.cuda.is_available(), reason="CUDA is required for tower placement checks.")
+def test_policy_chunk_switches_generator_and_reasoner_placement(monkeypatch: pytest.MonkeyPatch):
+    class TinyLayer(torch.nn.Module):
+        def __init__(self) -> None:
+            super().__init__()
+            self.q_proj = torch.nn.Linear(4, 4, bias=False)
+            self.q_proj_moe_gen = torch.nn.Linear(4, 4, bias=False)
+
+    net = torch.nn.Module()
+    net.language_model = torch.nn.Module()
+    net.language_model.model = torch.nn.Module()
+    net.language_model.model.layers = torch.nn.ModuleList([TinyLayer()])
+    net.language_model.model.embed_tokens = torch.nn.Embedding(8, 4)
+    net.language_model.model.norm = torch.nn.LayerNorm(4)
+    net.language_model.model.norm_moe_gen = torch.nn.LayerNorm(4)
+    net.language_model.lm_head = torch.nn.Linear(4, 8, bias=False)
+
+    model = OmniMoTModel.__new__(OmniMoTModel)
+    torch.nn.Module.__init__(model)
+    model._reasoner_offload_complete = True
+    monkeypatch.setenv("COSMOS3_REASONER_OFFLOAD", "1")
+
+    # Recreate the end-of-chunk state: UND layers on CPU while GEN and the
+    # non-layer reasoner bridge remain on CUDA.
+    for name, parameter in net.named_parameters():
+        if model._reasoner_generator_parameter_target(name) == "cuda" or not name.startswith(
+            "language_model.model.layers."
+        ):
+            parameter.data = parameter.data.cuda()
+
+    model._prepare_reasoner_prefill(net)
+    assert not model._reasoner_offload_complete
+    for name, parameter in net.named_parameters():
+        placement = model._reasoner_generator_parameter_target(name)
+        if placement == "cuda":
+            assert parameter.device.type == "cuda", name
+        elif placement == "cpu" and not name.startswith("language_model.model.layers."):
+            assert parameter.device.type == "cuda", name
+
+    model._offload_reasoner_and_pin_generator(net)
+    assert model._reasoner_offload_complete
+    for name, parameter in net.named_parameters():
+        placement = model._reasoner_generator_parameter_target(name)
+        if placement == "cuda":
+            assert parameter.device.type == "cuda", name
+        elif placement == "cpu" and name.startswith("language_model.model.layers."):
+            assert parameter.device.type == "cpu", name
+        elif placement == "cpu":
+            assert parameter.device.type == "cuda", name
diff --git a/cosmos_framework/model/generator/mot/unified_mot.py b/cosmos_framework/model/generator/mot/unified_mot.py
index 2bdebfd..9b4421a 100644
--- a/cosmos_framework/model/generator/mot/unified_mot.py
+++ b/cosmos_framework/model/generator/mot/unified_mot.py
@@ -11,9 +11,19 @@ import torch
 from torch import nn
 from torch.distributed import ProcessGroup
 
+from cosmos_framework.data.generator.sequence_packing.runtime import (
+    SequencePack,
+    from_all_seq,
+    from_und_gen_splits,
+    get_device_and_dtype,
+    get_gen_seq,
+    get_und_seq,
+    set_gen_seq,
+    set_und_seq,
+    zeros_like,
+)
 from cosmos_framework.model.attention import attention as imaginaire_attention
 from cosmos_framework.model.attention.masks import CausalType
-from cosmos_framework.utils import log
 from cosmos_framework.model.generator.mot.attention import (
     AttentionMaskType,
     dispatch_attention,
@@ -67,17 +77,7 @@ from cosmos_framework.model.generator.reasoner.qwen3_vl_moe.qwen3_vl_moe import
     Qwen3VLMoeVisionModel,
 )
 from cosmos_framework.model.generator.utils.memory import KVToStore, MemoryState, MemoryValue
-from cosmos_framework.data.generator.sequence_packing.runtime import (
-    SequencePack,
-    from_all_seq,
-    from_und_gen_splits,
-    get_device_and_dtype,
-    get_gen_seq,
-    get_und_seq,
-    set_gen_seq,
-    set_und_seq,
-    zeros_like,
-)
+from cosmos_framework.utils import log
 
 # Torch optimization settings
 torch._dynamo.config.cache_size_limit = 512
@@ -570,6 +570,7 @@ class PackedAttentionMoT(nn.Module):
         packed_position_embeddings: tuple[SequencePack, SequencePack],
         natten_metadata: dict | None = None,
         memory_value: MemoryValue | None = None,
+        und_only: bool = False,
     ) -> tuple[SequencePack, KVToStore | None]:
         """Forward pass with optional memory-augmented attention.
 
@@ -591,39 +592,85 @@ class PackedAttentionMoT(nn.Module):
             memory_value: Optional read-only tensor container for memory-augmented attention.
         """
 
-        q_und_in = self.q_proj(get_und_seq(pack))  # [N_und,num_heads*head_dim]
-        q_gen_in = self.q_proj_moe_gen(get_gen_seq(pack))  # [N_gen,num_heads*head_dim]
+        has_cached_und = memory_value is not None and getattr(memory_value, "und_k", None) is not None
+        if und_only:
+            if memory_value is None or has_cached_und:
+                raise ValueError("und_only diffusion prefill requires an empty MemoryValue.")
+
+            und = get_und_seq(pack)
+            q_und = self.q_proj(und).view(-1, self.num_attention_heads, self.head_dim)
+            k_und = self.k_proj(und).view(-1, self.num_key_value_heads, self.head_dim)
+            v_und = self.v_proj(und).view(-1, self.num_key_value_heads, self.head_dim)
+            q_und = self.q_norm(q_und)
+            k_und = self.k_norm(k_und)
+
+            packed_cos, packed_sin = packed_position_embeddings
+            q_und_, k_und_ = self._apply_rotary_pos_emb(
+                q_und,
+                k_und,
+                get_und_seq(packed_cos),
+                get_und_seq(packed_sin),
+                unsqueeze_dim=1,
+            )
 
-        k_und_in = self.k_proj(get_und_seq(pack))  # [N_und,num_kv_heads*head_dim]
-        k_gen_in = self.k_proj_moe_gen(get_gen_seq(pack))  # [N_gen,num_kv_heads*head_dim]
+            if self.k_norm_und_for_gen is not None:
+                k_und_for_gen = self.k_norm_und_for_gen(k_und)
+                _, k_und_for_gen_ = self._apply_rotary_pos_emb(
+                    q_und_,
+                    k_und_for_gen,
+                    get_und_seq(packed_cos),
+                    get_und_seq(packed_sin),
+                    unsqueeze_dim=1,
+                )
+            else:
+                k_und_for_gen_ = k_und_
+
+            causal_offsets = pack["_causal_seq_offsets"]
+            num_samples = causal_offsets.shape[0] - 1
+            causal_kwargs = {}
+            if num_samples != 1 or torch.is_grad_enabled():
+                causal_kwargs = {
+                    "cumulative_seqlen_Q": causal_offsets,
+                    "cumulative_seqlen_KV": causal_offsets,
+                    "max_seqlen_Q": pack["max_causal_len"],
+                    "max_seqlen_KV": pack["max_causal_len"],
+                }
+            causal_out = imaginaire_attention(
+                q_und_.unsqueeze(0),
+                k_und_.unsqueeze(0),
+                v_und.unsqueeze(0),
+                is_causal=True,
+                causal_type=CausalType.DontCare,
+                **causal_kwargs,
+            ).squeeze(0)
+            und_seq = self.o_proj(causal_out.flatten(-2, -1))
+            gen_seq = und_seq.new_empty((0, self.hidden_size))
 
-        v_und_in = self.v_proj(get_und_seq(pack))  # [N_und,num_kv_heads*head_dim]
-        v_gen_in = self.v_proj_moe_gen(get_gen_seq(pack))  # [N_gen,num_kv_heads*head_dim]
+            und_len = pack["_num_causal_tokens"]
+            empty_gen_k = k_und_.new_empty((1, 0, self.num_key_value_heads, self.head_dim))
+            empty_gen_v = v_und.new_empty((1, 0, self.num_key_value_heads, self.head_dim))
+            kv_to_store = (
+                empty_gen_k,
+                empty_gen_v,
+                k_und_for_gen_[:und_len].unsqueeze(0),
+                v_und[:und_len].unsqueeze(0),
+            )
+            return from_und_gen_splits(und_seq, gen_seq, pack), kv_to_store
 
-        q_und = q_und_in.view(-1, self.num_attention_heads, self.head_dim)  # [N_und,num_heads,head_dim]
-        k_und = k_und_in.view(-1, self.num_key_value_heads, self.head_dim)  # [N_und,num_kv_heads,head_dim]
-        v_und = v_und_in.view(-1, self.num_key_value_heads, self.head_dim)  # [N_und,num_kv_heads,head_dim]
+        q_gen_in = self.q_proj_moe_gen(get_gen_seq(pack))  # [N_gen,num_heads*head_dim]
+        k_gen_in = self.k_proj_moe_gen(get_gen_seq(pack))  # [N_gen,num_kv_heads*head_dim]
+        v_gen_in = self.v_proj_moe_gen(get_gen_seq(pack))  # [N_gen,num_kv_heads*head_dim]
 
         q_gen = q_gen_in.view(-1, self.num_attention_heads, self.head_dim)  # [N_gen,num_heads,head_dim]
         k_gen = k_gen_in.view(-1, self.num_key_value_heads, self.head_dim)  # [N_gen,num_kv_heads,head_dim]
         v_gen = v_gen_in.view(-1, self.num_key_value_heads, self.head_dim)  # [N_gen,num_kv_heads,head_dim]
 
-        q_und = self.q_norm(q_und)  # [N_und,num_heads,head_dim]
-        k_und = self.k_norm(k_und)  # [N_und,num_kv_heads,head_dim]
-
         q_gen = self.q_norm_moe_gen(q_gen)  # [N_gen,num_heads,head_dim]
         k_gen = self.k_norm_moe_gen(k_gen)  # [N_gen,num_kv_heads,head_dim]
 
         packed_cos = packed_position_embeddings[0]
         packed_sin = packed_position_embeddings[1]
 
-        q_und_, k_und_ = self._apply_rotary_pos_emb(
-            q_und,
-            k_und,
-            get_und_seq(packed_cos),
-            get_und_seq(packed_sin),
-            unsqueeze_dim=1,
-        )  # q_und_: [N_und,num_heads,head_dim], k_und_: [N_und,num_kv_heads,head_dim]
         q_gen_, k_gen_ = self._apply_rotary_pos_emb(
             q_gen,
             k_gen,
@@ -632,26 +679,49 @@ class PackedAttentionMoT(nn.Module):
             unsqueeze_dim=1,
         )  # q_gen_: [N_gen,num_heads,head_dim], k_gen_: [N_gen,num_kv_heads,head_dim]
 
-        packed_query_states_ = from_und_gen_splits(q_und_, q_gen_, pack)  # [N_und+N_gen,num_heads,head_dim]
-        packed_key_states_ = from_und_gen_splits(k_und_, k_gen_, pack)  # [N_und+N_gen,num_kv_heads,head_dim]
-        packed_value_states_ = from_und_gen_splits(v_und, v_gen, pack)  # [N_und+N_gen,num_kv_heads,head_dim]
-
-        # Build a separate K pack where the und tokens are normalised for gen→und
-        # cross-attention (fixes scale mismatch when qk_norm_for_diffusion=True but
-        # qk_norm_for_text=False, i.e. the Nemotron-3 config).  The raw k_und_ is kept
-        # in packed_key_states_ for the reasoner's own causal self-attention.
-        if self.k_norm_und_for_gen is not None:
-            k_und_normalized = self.k_norm_und_for_gen(k_und)  # RMSNorm before RoPE
-            _, k_und_for_gen_ = self._apply_rotary_pos_emb(
-                q_und_,  # dummy q — already RoPE-applied, output discarded; only k is used
-                k_und_normalized,
+        if has_cached_und:
+            # Diffusion gen-only: the reasoner projection/norm modules may be
+            # offloaded to CPU.  Empty CUDA inputs would still trigger device
+            # checks inside Linear/RMSNorm, so do not call them at all.
+            q_und_ = q_gen_.new_empty((0, self.num_attention_heads, self.head_dim))
+            k_und_ = k_gen_.new_empty((0, self.num_key_value_heads, self.head_dim))
+            v_und = v_gen.new_empty((0, self.num_key_value_heads, self.head_dim))
+            packed_key_states_normalized_: SequencePack | None = None
+        else:
+            q_und_in = self.q_proj(get_und_seq(pack))  # [N_und,num_heads*head_dim]
+            k_und_in = self.k_proj(get_und_seq(pack))  # [N_und,num_kv_heads*head_dim]
+            v_und_in = self.v_proj(get_und_seq(pack))  # [N_und,num_kv_heads*head_dim]
+            q_und = q_und_in.view(-1, self.num_attention_heads, self.head_dim)
+            k_und = k_und_in.view(-1, self.num_key_value_heads, self.head_dim)
+            v_und = v_und_in.view(-1, self.num_key_value_heads, self.head_dim)
+            q_und = self.q_norm(q_und)
+            k_und = self.k_norm(k_und)
+            q_und_, k_und_ = self._apply_rotary_pos_emb(
+                q_und,
+                k_und,
                 get_und_seq(packed_cos),
                 get_und_seq(packed_sin),
                 unsqueeze_dim=1,
-            )  # k_und_for_gen_: [N_und,num_kv_heads,head_dim]
-            packed_key_states_normalized_: SequencePack | None = from_und_gen_splits(k_und_for_gen_, k_gen_, pack)
-        else:
-            packed_key_states_normalized_ = None
+            )
+
+            # Build a separate K pack where the und tokens are normalised for
+            # gen→und cross-attention while causal self-attention keeps raw K.
+            if self.k_norm_und_for_gen is not None:
+                k_und_normalized = self.k_norm_und_for_gen(k_und)
+                _, k_und_for_gen_ = self._apply_rotary_pos_emb(
+                    q_und_,
+                    k_und_normalized,
+                    get_und_seq(packed_cos),
+                    get_und_seq(packed_sin),
+                    unsqueeze_dim=1,
+                )
+                packed_key_states_normalized_ = from_und_gen_splits(k_und_for_gen_, k_gen_, pack)
+            else:
+                packed_key_states_normalized_ = None
+
+        packed_query_states_ = from_und_gen_splits(q_und_, q_gen_, pack)  # [N_und+N_gen,num_heads,head_dim]
+        packed_key_states_ = from_und_gen_splits(k_und_, k_gen_, pack)  # [N_und+N_gen,num_kv_heads,head_dim]
+        packed_value_states_ = from_und_gen_splits(v_und, v_gen, pack)  # [N_und+N_gen,num_kv_heads,head_dim]
 
         packed_attn_output, kv_to_store = self.dispatch_attention_fn(
             packed_query_states_,
@@ -671,7 +741,7 @@ class PackedAttentionMoT(nn.Module):
         # Gradient detach is NOT done here; each MemoryState.write_for_layer()
         # decides its own gradient policy (e.g. detach for truncated BPTT,
         # keep gradients for teacher forcing).
-        if memory_value is not None and kv_to_store is None:
+        if memory_value is not None and not has_cached_und and kv_to_store is None:
             und_len = pack["_num_causal_tokens"]
             gen_len = pack["_num_full_tokens"]
             # When und K-norm is active, AR frame 1+ gen→und cross-attention uses
@@ -717,7 +787,11 @@ class PackedAttentionMoT(nn.Module):
                 cp_group,
             )  # [N_gen,hidden_size]
         else:
-            und_seq = self.o_proj(get_und_seq(packed_attn_output))  # [N_und,hidden_size]
+            if has_cached_und:
+                gen_attn = get_gen_seq(packed_attn_output)
+                und_seq = gen_attn.new_empty((0, self.hidden_size))
+            else:
+                und_seq = self.o_proj(get_und_seq(packed_attn_output))  # [N_und,hidden_size]
             gen_seq = self.o_proj_moe_gen(get_gen_seq(packed_attn_output))  # [N_gen,hidden_size]
         return from_und_gen_splits(und_seq, gen_seq, pack), kv_to_store  # [N_und+N_gen,hidden_size]
 
@@ -869,6 +943,7 @@ def _impl_forward(
     position_ids: torch.Tensor,
     natten_metadata_list: list | None = None,
     memory: MemoryState | None = None,
+    und_only: bool = False,
 ) -> tuple[SequencePack, dict[str, LBLMetadata]]:
     """Shared training forward pass for the three MoT text models.
 
@@ -883,6 +958,7 @@ def _impl_forward(
         natten_metadata_list: Optional per-layer NATTEN metadata.
         memory: Optional ``MemoryState`` for persistent memory across
             forward passes.
+        und_only: Run only the understanding pathway and populate ``memory``.
     """
 
     # Create position embeddings (Qwen3 style) - squeeze once at model level
@@ -909,14 +985,103 @@ def _impl_forward(
 
     hidden_states = pack
 
+    # ---- Layer-wise CPU offload (4090; env COSMOS3_LAYER_OFFLOAD) ----
+    # Stream each transformer layer to the inference GPU for its forward, then
+    # evict back to CPU. Eager only (compile disabled in build_net). Synchronous
+    # first cut; the async two-stream overlap is a follow-up.
+    import os as _os
+
+    def _offload_env_enabled(name: str) -> bool:
+        return _os.environ.get(name, "").strip().lower() not in {"", "0", "false", "no", "off"}
+
+    _reasoner_offload = _offload_env_enabled("COSMOS3_REASONER_OFFLOAD")
+    _layer_offload = _offload_env_enabled("COSMOS3_LAYER_OFFLOAD") or _reasoner_offload
+
+    def _stage_layer_path_to_cuda(
+        layer: torch.nn.Module, *, generator: bool, target: torch.device | str
+    ) -> tuple[int, list[tuple[torch.Tensor, torch.Tensor]]]:
+        target_device = torch.device(target)
+        if target_device.type != "cuda":
+            raise ValueError(f"Layer staging target must be CUDA, got {target_device}.")
+        moved_bytes = 0
+        backups: list[tuple[torch.Tensor, torch.Tensor]] = []
+
+        def _is_on_target(tensor: torch.Tensor) -> bool:
+            return tensor.device.type == target_device.type and (
+                target_device.index is None or tensor.device.index == target_device.index
+            )
+
+        for name, parameter in layer.named_parameters():
+            if ("_moe_gen" in name) == generator and not _is_on_target(parameter):
+                cpu_master = parameter.data
+                moved_bytes += parameter.numel() * parameter.element_size()
+                parameter.data = cpu_master.to(target_device, non_blocking=False)
+                backups.append((parameter, cpu_master))
+        for name, buffer in layer.named_buffers():
+            if ("_moe_gen" in name) == generator and not _is_on_target(buffer):
+                cpu_master = buffer.data
+                moved_bytes += buffer.numel() * buffer.element_size()
+                buffer.data = cpu_master.to(target_device, non_blocking=False)
+                backups.append((buffer, cpu_master))
+        return moved_bytes, backups
+
+    def _restore_layer_path_from_cpu_master(backups: list[tuple[torch.Tensor, torch.Tensor]]) -> int:
+        restored_bytes = 0
+        for tensor, cpu_master in reversed(backups):
+            restored_bytes += cpu_master.numel() * cpu_master.element_size()
+            tensor.data = cpu_master
+        return restored_bytes
+
     # --- MemoryState: per-step init (outside compile) ---
     if memory is not None:
         memory.init(hidden_states, device)
 
     # Derive gen_only once (outside compile) if using MemoryState
     memory_gen_only = memory.is_gen_only() if memory is not None else False
+    if und_only and memory_gen_only:
+        raise ValueError("und_only cannot run with an already populated diffusion cache.")
+    # The first full pass streams complete layers.  The gen-only denoising
+    # pass runs only after OmniMoTModel has pinned all ``*_moe_gen`` modules.
+    _stream_reasoner_layers = _reasoner_offload and und_only
+    _stream_complete_layers = _layer_offload and not und_only and not (_reasoner_offload and memory_gen_only)
+    generator_devices_before: dict[str, torch.device] = {}
+    generator_total_bytes = 0
+    generator_cuda_bytes = 0
+    reasoner_h2d_bytes = 0
+    reasoner_d2h_bytes = 0
+    reasoner_h2d_layers = 0
+    reasoner_d2h_layers = 0
+    reasoner_restore_layers = 0
+    if _stream_reasoner_layers:
+        for layer_idx, layer in enumerate(self.layers):
+            for name, parameter in layer.named_parameters():
+                if "_moe_gen" in name:
+                    key = f"layers.{layer_idx}.{name}"
+                    generator_devices_before[key] = parameter.device
+                    size = parameter.numel() * parameter.element_size()
+                    generator_total_bytes += size
+                    if parameter.device.type == "cuda":
+                        generator_cuda_bytes += size
+            for name, buffer in layer.named_buffers():
+                if "_moe_gen" in name:
+                    key = f"layers.{layer_idx}.{name}#buffer"
+                    generator_devices_before[key] = buffer.device
+                    size = buffer.numel() * buffer.element_size()
+                    generator_total_bytes += size
+                    if buffer.device.type == "cuda":
+                        generator_cuda_bytes += size
 
     for i, decoder_layer in enumerate(self.layers):
+        reasoner_cpu_backups: list[tuple[torch.Tensor, torch.Tensor]] = []
+        if _stream_reasoner_layers:
+            moved_bytes, reasoner_cpu_backups = _stage_layer_path_to_cuda(
+                decoder_layer, generator=False, target=device
+            )
+            reasoner_h2d_bytes += moved_bytes
+            reasoner_h2d_layers += int(moved_bytes > 0)
+        elif _stream_complete_layers:
+            decoder_layer.to(device)  # H2D: pull layer i onto the inference GPU
+
         # MemoryState: produce read-only MemoryValue for this layer (outside compile)
         memory_value = memory.read_for_layer(i) if memory is not None else None
 
@@ -927,8 +1092,19 @@ def _impl_forward(
             natten_metadata=None if natten_metadata_list is None else natten_metadata_list[i],
             memory_value=memory_value,
             gen_only=memory_gen_only,
+            und_only=und_only,
         )
 
+        if _stream_reasoner_layers:
+            restored_bytes = _restore_layer_path_from_cpu_master(reasoner_cpu_backups)
+            reasoner_restore_layers += int(restored_bytes > 0)
+        elif _stream_complete_layers:
+            # During prefill, evict the complete mixed layer.  This guarantees
+            # that each policy chunk has a true phase boundary: generator on
+            # CPU while reasoner layers stream, then reasoner on CPU while the
+            # complete generator is made resident for denoising.
+            decoder_layer.to("cpu")
+
         # MemoryState: store K/V produced by this layer (outside compile)
         if kv_to_store is not None and memory is not None:
             memory.write_for_layer(i, kv_to_store)
@@ -936,6 +1112,33 @@ def _impl_forward(
         for pathway, lbl_metadata in lbl_metadata_dict.items():
             lbl_metadata_all[pathway].append(lbl_metadata)
 
+    if _stream_reasoner_layers:
+        for layer_idx, layer in enumerate(self.layers):
+            for name, parameter in layer.named_parameters():
+                if "_moe_gen" in name and parameter.device != generator_devices_before[f"layers.{layer_idx}.{name}"]:
+                    raise RuntimeError(
+                        "UND-only prefill changed generator tensor placement: "
+                        f"layers.{layer_idx}.{name} was {generator_devices_before[f'layers.{layer_idx}.{name}']} "
+                        f"and is now {parameter.device}"
+                    )
+            for name, buffer in layer.named_buffers():
+                key = f"layers.{layer_idx}.{name}#buffer"
+                if "_moe_gen" in name and buffer.device != generator_devices_before[key]:
+                    raise RuntimeError(
+                        f"UND-only prefill changed generator buffer placement: {key} "
+                        f"was {generator_devices_before[key]} and is now {buffer.device}"
+                    )
+        log.info(
+            f"UnifiedMoT: UND-only prefill complete for {len(self.layers)} layers; "
+            f"reasoner_h2d_layers={reasoner_h2d_layers} "
+            f"reasoner_h2d={reasoner_h2d_bytes / (1024**3):.2f} GiB "
+            f"reasoner_d2h_layers={reasoner_d2h_layers} "
+            f"reasoner_d2h={reasoner_d2h_bytes / (1024**3):.2f} GiB; "
+            f"reasoner_restore_layers={reasoner_restore_layers}; "
+            f"generator placement unchanged with "
+            f"{generator_cuda_bytes / (1024**3):.2f}/{generator_total_bytes / (1024**3):.2f} GiB on CUDA"
+        )
+
     # Compute the load balancing loss across all layers. For dense models, final_lbl_metadata
     # will be an empty dictionary. For MoE models, it will be a dictionary with the stacked
     # load balancing loss metadata for each pathway.
@@ -956,8 +1159,17 @@ def _impl_forward(
             )
 
     hidden_states_out = zeros_like(hidden_states)
-    set_und_seq(hidden_states_out, self.norm(get_und_seq(hidden_states)))  # [N_und,hidden_size]
-    set_gen_seq(hidden_states_out, self.norm_moe_gen(get_gen_seq(hidden_states)))  # [N_gen,hidden_size]
+    if und_only:
+        und_hidden = get_und_seq(hidden_states)
+        set_und_seq(hidden_states_out, und_hidden)
+        set_gen_seq(hidden_states_out, und_hidden.new_empty((0, und_hidden.shape[-1])))
+    elif memory_gen_only:
+        gen_hidden = get_gen_seq(hidden_states)
+        set_und_seq(hidden_states_out, gen_hidden.new_empty((0, gen_hidden.shape[-1])))
+    else:
+        set_und_seq(hidden_states_out, self.norm(get_und_seq(hidden_states)))  # [N_und,hidden_size]
+    if not und_only:
+        set_gen_seq(hidden_states_out, self.norm_moe_gen(get_gen_seq(hidden_states)))  # [N_gen,hidden_size]
 
     return hidden_states_out, final_lbl_metadata
 
@@ -1041,6 +1253,7 @@ class MoTDecoderLayer(nn.Module):
         natten_metadata: dict | None = None,
         memory_value: MemoryValue | None = None,
         gen_only: bool = False,
+        und_only: bool = False,
     ) -> tuple[SequencePack, dict[str, LBLMetadata], KVToStore | None]:
         """Forward pass with MoT routing and optional memory-augmented attention.
 
@@ -1057,24 +1270,36 @@ class MoTDecoderLayer(nn.Module):
             natten_metadata: Optional NATTEN metadata for neighborhood attention.
             memory_value: Read-only tensor container from MemoryState.read_for_layer().
             gen_only: When True, skip the understanding pathway (und K/V come from cache).
+            und_only: When True, run only the understanding pathway and populate its K/V cache.
         """
-        # Pre-Attention layernorm
-        pack_norm_out = from_und_gen_splits(
-            self.input_layernorm(get_und_seq(input)),  # [N_und,hidden_size]
-            self.input_layernorm_moe_gen(get_gen_seq(input)),  # [N_gen,hidden_size]
-            input,
-        )  # [N_und+N_gen,hidden_size]
-
+        if gen_only and und_only:
+            raise ValueError("gen_only and und_only are mutually exclusive.")
         # Self Attention + Residual
         kv_to_store: KVToStore | None = None
-        if gen_only:
+        if und_only:
+            und_norm = self.input_layernorm(get_und_seq(input))
+            und_pack = from_und_gen_splits(
+                und_norm,
+                und_norm.new_empty((0, und_norm.shape[-1])),
+                input,
+            )
+            pack_attn_out, kv_to_store = self.self_attn(
+                und_pack,
+                attention_mask,
+                packed_position_embeddings,
+                natten_metadata=None,
+                memory_value=memory_value,
+                und_only=True,
+            )
+            residual_und = get_und_seq(input) + get_und_seq(pack_attn_out)
+        elif gen_only:
             assert natten_metadata is None
             # gen_only: skip und, compute gen tokens only (und K/V come from cache)
-            _gen_norm = get_gen_seq(pack_norm_out)
+            _gen_norm = self.input_layernorm_moe_gen(get_gen_seq(input))
             gen_pack = from_und_gen_splits(
                 _gen_norm.new_empty(0, _gen_norm.shape[-1]),
                 _gen_norm,
-                pack_norm_out,
+                input,
             )
 
             # Build position embeddings whose und length matches gen_pack's
@@ -1107,6 +1332,11 @@ class MoTDecoderLayer(nn.Module):
             residual_gen = get_gen_seq(input) + gen_attn_out
         else:
             # STANDARD PATH: Process both und and gen tokens
+            pack_norm_out = from_und_gen_splits(
+                self.input_layernorm(get_und_seq(input)),
+                self.input_layernorm_moe_gen(get_gen_seq(input)),
+                input,
+            )
             pack_attn_out, kv_to_store = self.self_attn(
                 pack_norm_out,
                 attention_mask,
@@ -1120,7 +1350,17 @@ class MoTDecoderLayer(nn.Module):
         # Pre-MLP layernorm and processing
         lbl_metadata_dict: dict[str, LBLMetadata] = dict()
 
-        if gen_only:
+        if und_only:
+            ln_out_und = self.post_attention_layernorm(residual_und)
+            und_len = pack_attn_out["_num_causal_tokens"]
+            ln_out_und_unpadded = ln_out_und[:und_len]
+            mlp_out_und_unpadded, lbl_metadata_und = _run_mlp(self.mlp, ln_out_und_unpadded)
+            mlp_out_und = torch.cat([mlp_out_und_unpadded, ln_out_und[und_len:]], dim=0)
+            if lbl_metadata_und is not None:
+                lbl_metadata_dict["und"] = lbl_metadata_und
+            mlp_out_und_seq = residual_und + mlp_out_und
+            mlp_out_gen_seq = residual_und.new_empty((0, residual_und.shape[-1]))
+        elif gen_only:
             # gen_only: skip und, compute gen tokens only
             ln_out_und = residual_gen.new_empty(0, residual_gen.shape[-1])
             ln_out_gen = self.post_attention_layernorm_moe_gen(residual_gen)
@@ -2049,6 +2289,7 @@ class Qwen3VLTextForCausalLM(Qwen3VLPreTrainedModel):
         position_ids: torch.Tensor,
         natten_metadata_list: list | None = None,
         memory: MemoryState | None = None,
+        und_only: bool = False,
     ) -> tuple[SequencePack, dict[str, LBLMetadata]]:
         """Training forward pass — delegates to the dense text model."""
         outputs = self.model(
@@ -2057,6 +2298,7 @@ class Qwen3VLTextForCausalLM(Qwen3VLPreTrainedModel):
             position_ids=position_ids,
             natten_metadata_list=natten_metadata_list,
             memory=memory,
+            und_only=und_only,
         )
         return outputs
 
@@ -2188,6 +2430,7 @@ class Qwen3VLMoeTextForCausalLM(Qwen3VLMoePreTrainedModel):
         position_ids: torch.Tensor,
         natten_metadata_list: list | None = None,
         memory: MemoryState | None = None,
+        und_only: bool = False,
     ) -> tuple[SequencePack, dict[str, LBLMetadata]]:
         """Training forward pass — delegates to the MoE text model."""
 
@@ -2197,6 +2440,7 @@ class Qwen3VLMoeTextForCausalLM(Qwen3VLMoePreTrainedModel):
             position_ids=position_ids,
             natten_metadata_list=natten_metadata_list,
             memory=memory,
+            und_only=und_only,
         )
 
         return outputs
@@ -2323,6 +2567,7 @@ class Nemotron3DenseVLTextForCausalLM(Nemotron3DenseVLPreTrainedModel):
         position_ids: torch.Tensor,
         natten_metadata_list: list | None = None,
         memory: MemoryState | None = None,
+        und_only: bool = False,
     ) -> tuple[SequencePack, dict[str, LBLMetadata]]:
         return self.model(
             pack=pack,
@@ -2330,6 +2575,7 @@ class Nemotron3DenseVLTextForCausalLM(Nemotron3DenseVLPreTrainedModel):
             position_ids=position_ids,
             natten_metadata_list=natten_metadata_list,
             memory=memory,
+            und_only=und_only,
         )
 
     def generate_reasoner_text(
diff --git a/cosmos_framework/model/generator/omni_mot_model.py b/cosmos_framework/model/generator/omni_mot_model.py
index 656364b..55a4d14 100644
--- a/cosmos_framework/model/generator/omni_mot_model.py
+++ b/cosmos_framework/model/generator/omni_mot_model.py
@@ -4,9 +4,12 @@
 from __future__ import annotations
 
 import collections
+import hashlib
 import json
+import os
 import time
 from contextlib import contextmanager
+from pathlib import Path
 from typing import Any, Callable, Dict, Mapping, Optional, Tuple
 
 import numpy as np
@@ -16,27 +19,32 @@ from einops import rearrange
 from torch.distributed._composable.fsdp import FSDPModule
 from torch.nn.modules.module import _IncompatibleKeys
 
-from cosmos_framework.utils.flags import DEVICE, TRAINING, Device
-from cosmos_framework.utils.lazy_config import LazyDict
-from cosmos_framework.utils.lazy_config import instantiate as lazy_instantiate
-from cosmos_framework.model._base import ImaginaireModel
-from cosmos_framework.utils import log, misc
-from cosmos_framework.utils.count_params import count_params
-from cosmos_framework.utils.timer import Timer
-from cosmos_framework.model.generator.algorithm.loss.flow_matching import compute_flow_matching_loss
-from cosmos_framework.model.generator.algorithm.loss.load_balancing import compute_load_balancing_loss
 from cosmos_framework.configs.base.defaults.model_config import OmniMoTModelConfig
 from cosmos_framework.data.generator.action.action_processing import ActionProcessor, get_action_processing_records
+from cosmos_framework.data.generator.sequence_packing import (
+    PackedSequence,
+    SequencePlan,
+    build_sequence_plans_from_data_batch,
+    pack_input_sequence,
+)
+from cosmos_framework.data.generator.sequence_packing.modalities import add_special_tokens
 from cosmos_framework.data.generator.utils import IMAGE_RES_SIZE_INFO, VIDEO_RES_SIZE_INFO
+from cosmos_framework.model._base import ImaginaireModel
+from cosmos_framework.model.generator.algorithm.loss.flow_matching import compute_flow_matching_loss
+from cosmos_framework.model.generator.algorithm.loss.load_balancing import compute_load_balancing_loss
 from cosmos_framework.model.generator.diffusion.rectified_flow import RectifiedFlow
 from cosmos_framework.model.generator.diffusion.samplers.edm import EDMSampler
 from cosmos_framework.model.generator.diffusion.samplers.fixed_step import FixedStepSampler
 from cosmos_framework.model.generator.diffusion.samplers.unipc import UniPCSampler, UniPCSamplerConfig
+from cosmos_framework.model.generator.mot.attention import dispatch_attention_with_memory
 from cosmos_framework.model.generator.mot.context_parallel_utils import context_parallel_broadcast_tensor_list
 from cosmos_framework.model.generator.mot.cosmos3_vfm_network import Cosmos3VFMNetwork, Cosmos3VFMNetworkConfig
 from cosmos_framework.model.generator.mot.modeling_utils import has_noisy_tokens
 from cosmos_framework.model.generator.mot.parallelize_vfm_network import parallelize_vfm_network
 from cosmos_framework.model.generator.reasoner.qwen3_vl.utils import tokenize_caption
+from cosmos_framework.model.generator.tokenizers.interface import VideoTokenizerInterface
+from cosmos_framework.model.generator.upsampler.prompts import build_messages, clean_response
+from cosmos_framework.model.generator.utils.condition_only_vae import encode_first_condition_frame
 from cosmos_framework.model.generator.utils.data_and_condition import (
     GenerationDataClean,
     GenerationDataNoised,
@@ -44,23 +52,20 @@ from cosmos_framework.model.generator.utils.data_and_condition import (
     build_dense_sound_schedule,
     unwrap_and_densify,
 )
-from cosmos_framework.model.generator.utils.memory import MemoryState
+from cosmos_framework.model.generator.utils.memory import DiffusionKVCacheMemoryState, MemoryState
 from cosmos_framework.model.generator.utils.safetensors_loader import (
     load_language_model as load_language_model_safetensors,
 )
-from cosmos_framework.data.generator.sequence_packing import (
-    PackedSequence,
-    SequencePlan,
-    build_sequence_plans_from_data_batch,
-    pack_input_sequence,
-)
-from cosmos_framework.data.generator.sequence_packing.modalities import add_special_tokens
-from cosmos_framework.model.generator.tokenizers.interface import VideoTokenizerInterface
-from cosmos_framework.model.generator.upsampler.prompts import build_messages, clean_response
+from cosmos_framework.utils import log, misc
+from cosmos_framework.utils.count_params import count_params
+from cosmos_framework.utils.flags import DEVICE, TRAINING, Device
 from cosmos_framework.utils.generator.data_utils import get_vision_data_resolution
 from cosmos_framework.utils.generator.dtensor_helper import DTensorFastEmaModelUpdater
 from cosmos_framework.utils.generator.model_weights_stats import WeightTrainingStat
 from cosmos_framework.utils.generator.parallelism import ParallelDims
+from cosmos_framework.utils.lazy_config import LazyDict
+from cosmos_framework.utils.lazy_config import instantiate as lazy_instantiate
+from cosmos_framework.utils.timer import Timer
 
 
 class OmniMoTModel(ImaginaireModel):
@@ -72,6 +77,11 @@ class OmniMoTModel(ImaginaireModel):
     def __init__(self, config: OmniMoTModelConfig):
         super().__init__()
         self.config = config
+        self._reasoner_offload_complete = False
+        self._inference_text_token_cache: dict[str, tuple[list[list[int]], list[list[int]]]] = {}
+        self._inference_reasoner_memory_cache: collections.OrderedDict[
+            str, DiffusionKVCacheMemoryState
+        ] = collections.OrderedDict()
         log.info(f"OmniMoTModel: config {self.config}")
 
         # 0. Set up precision
@@ -172,7 +182,6 @@ class OmniMoTModel(ImaginaireModel):
         else:
             self.tokenizer_sound_gen = None
 
-
     def build_net(self, dtype: torch.dtype, *, lora_enabled: bool | None = None) -> torch.nn.Module:
         # Build model network and parallelize it.
         lora_enabled = self.config.lora_enabled if lora_enabled is None else lora_enabled
@@ -230,6 +239,19 @@ class OmniMoTModel(ImaginaireModel):
 
         self.install_attention_dispatch(net)
 
+        # ---- Layer-wise CPU offload anchor (4090 24GB; env COSMOS3_LAYER_OFFLOAD) ----
+        # The DiT (~23 GB BF16) does not fit on a single 4090 next to VAE/activations.
+        # When enabled: (1) run EAGER (disable torch.compile — moving a compiled
+        # block's weights device between calls would force retrace); (2) materialize
+        # on CPU; (3) pin the small non-layer parts to GPU and stream the 36
+        # transformer layers per-step (see the block loop in unified_mot.py).
+        _layer_offload = _env_enabled("COSMOS3_LAYER_OFFLOAD") or self._reasoner_offload_enabled()
+        if _layer_offload:
+            try:
+                self.config.compile.enabled = False
+            except Exception:
+                pass  # frozen config — apply_compile is still gated on .enabled
+
         net = parallelize_vfm_network(
             net,
             parallel_dims=self.parallel_dims,
@@ -238,17 +260,31 @@ class OmniMoTModel(ImaginaireModel):
             attention_io_layout=self.config.parallelism.attention_io_layout,
         )
 
+        _build_device = "cpu" if _layer_offload else DEVICE
         with misc.timer("meta to cuda and broadcast model states"):
             net = net.to(dtype=dtype)
-            net.to_empty(device=DEVICE)
+            net.to_empty(device=_build_device)
+            if _layer_offload:
+                # Establish the mixed-device empty storage before init_weights
+                # writes any parameter pages.  In reasoner-offload mode this
+                # means GEN is initialized directly on CUDA instead of first
+                # committing a 12.94 GiB CPU copy and moving it afterwards.
+                self._place_initial_offload_towers(net, allocate_empty=True, log_result=False)
             if DEVICE == Device.CUDA:
                 # Weight initialization is not needed for other devices (cpu,
                 # meta), since they are only for checkpoint conversion and smoke
                 # tests.
-                net.init_weights(buffer_device=DEVICE)
+                net.init_weights(buffer_device=_build_device)
                 if lora_enabled:
                     self._init_lora_weights_post_materialization(net)
 
+        if _layer_offload:
+            # init_weights may recreate a small number of buffers on
+            # ``buffer_device``. Re-apply placement after initialization; the
+            # large parameters are already on their final devices, so this
+            # correction does not copy either tower.
+            self._place_initial_offload_towers(net, allocate_empty=False, log_result=True)
+
         return net
 
     def load_pretrained_model_if_needed(
@@ -376,17 +412,25 @@ class OmniMoTModel(ImaginaireModel):
         """Install a custom attention dispatch function on the network.
 
         Called during ``build_net()`` after the network is constructed but
-        before parallelization.  The base implementation is a no-op;
-        ``OmniMoTCausalModel`` overrides this to install
-        ``dispatch_attention_with_memory`` on every attention layer.
+        before parallelization.  The base implementation keeps historical
+        behavior unless the diffusion KV-cache env is enabled.
         """
-        pass
+        if self._diffusion_kv_cache_enabled():
+            self._install_attention_dispatch_with_memory(net)
+
+    def _install_attention_dispatch_with_memory(self, net: torch.nn.Module) -> None:
+        installed = 0
+        for module in net.modules():
+            if hasattr(module, "dispatch_attention_fn"):
+                module.dispatch_attention_fn = dispatch_attention_with_memory
+                installed += 1
+        log.info(f"{type(self).__name__}: installed memory-aware attention dispatch on {installed} modules")
 
     def set_up_memory(self) -> None:
         """Initialize memory state used during training (e.g. KV caches).
 
-        The base implementation is a no-op.  ``OmniMoTCausalModel`` overrides
-        this to allocate a KV cache.
+        The base implementation is a no-op; diffusion inference KV-cache state
+        is created per sampling branch by ``build_inference_memory_state``.
         """
         pass
 
@@ -669,6 +713,381 @@ class OmniMoTModel(ImaginaireModel):
         """
         return None
 
+    def build_inference_memory_state(self, net: torch.nn.Module | None = None) -> MemoryState | None:
+        """Construct persistent memory for one inference branch.
+
+        The base model keeps historical no-memory behavior unless
+        ``COSMOS3_REASONER_KVCACHE`` or ``COSMOS3_REASONER_OFFLOAD`` is set.
+        """
+        if not self._diffusion_kv_cache_enabled():
+            return None
+        return DiffusionKVCacheMemoryState(num_layers=self._num_decoder_layers(net))
+
+    @staticmethod
+    def _instruction_cache_digest(payload: Mapping[str, Any]) -> str:
+        encoded = json.dumps(payload, sort_keys=True, separators=(",", ":"), ensure_ascii=False).encode("utf-8")
+        return hashlib.sha256(encoded).hexdigest()
+
+    def _instruction_memory_identity(
+        self,
+        *,
+        text_tokens: list[list[int]],
+        skip_text_tokens: bool,
+        namespace: str,
+        net: torch.nn.Module | None,
+    ) -> dict[str, Any]:
+        return {
+            "schema_version": 1,
+            "namespace": namespace,
+            "text_tokens": text_tokens,
+            "skip_text_tokens": bool(skip_text_tokens),
+            "num_layers": self._num_decoder_layers(net),
+            "precision": str(self.precision),
+        }
+
+    def _get_instruction_memory_state(
+        self,
+        *,
+        text_tokens: list[list[int]],
+        skip_text_tokens: bool,
+        data_batch: Mapping[str, Any],
+        net: torch.nn.Module | None,
+    ) -> tuple[MemoryState | None, str | None]:
+        """Get a prompt-specific Reasoner K/V cache from memory or disk."""
+        if not self._diffusion_kv_cache_enabled():
+            return None, None
+        if not bool(data_batch.get("inference_instruction_cache", False)):
+            return self.build_inference_memory_state(net=net), None
+
+        namespace = str(data_batch.get("inference_instruction_cache_namespace", ""))
+        identity = self._instruction_memory_identity(
+            text_tokens=text_tokens,
+            skip_text_tokens=skip_text_tokens,
+            namespace=namespace,
+            net=net,
+        )
+        cache_key = self._instruction_cache_digest(identity)
+        memory = self._inference_reasoner_memory_cache.get(cache_key)
+        if memory is not None:
+            self._inference_reasoner_memory_cache.move_to_end(cache_key)
+            log.info(f"OmniMoTModel: instruction Reasoner K/V memory cache hit ({cache_key[:12]})")
+            return memory, cache_key
+
+        cache_dir_raw = data_batch.get("inference_instruction_cache_dir")
+        disk_cache_allowed = cache_dir_raw is not None and bool(namespace)
+        if torch.distributed.is_initialized() and torch.distributed.get_world_size() > 1:
+            disk_cache_allowed = False
+        if disk_cache_allowed:
+            cache_path = Path(str(cache_dir_raw)).expanduser() / "reasoner_kv" / f"{cache_key}.pt"
+            if cache_path.is_file():
+                try:
+                    payload = torch.load(cache_path, map_location="cpu", weights_only=True)
+                    if not isinstance(payload, dict) or payload.get("identity") != identity:
+                        raise ValueError("cache identity does not match the active model/prompt")
+                    memory = DiffusionKVCacheMemoryState.from_cpu_payload(
+                        payload["memory"],
+                        device=self.tensor_kwargs["device"],
+                        expected_num_layers=self._num_decoder_layers(net),
+                    )
+                    self._remember_instruction_memory(cache_key, memory, data_batch)
+                    log.info(f"OmniMoTModel: instruction Reasoner K/V disk cache hit: {cache_path}")
+                    return memory, cache_key
+                except Exception as exc:
+                    log.warning(f"Ignoring invalid instruction Reasoner K/V cache {cache_path}: {exc}")
+
+        memory = self.build_inference_memory_state(net=net)
+        return memory, cache_key
+
+    def _remember_instruction_memory(
+        self,
+        cache_key: str,
+        memory: DiffusionKVCacheMemoryState,
+        data_batch: Mapping[str, Any],
+    ) -> None:
+        self._inference_reasoner_memory_cache[cache_key] = memory
+        self._inference_reasoner_memory_cache.move_to_end(cache_key)
+        max_entries = max(1, int(data_batch.get("inference_instruction_cache_max_entries", 8)))
+        while len(self._inference_reasoner_memory_cache) > max_entries:
+            self._inference_reasoner_memory_cache.popitem(last=False)
+
+    def _persist_instruction_memory(
+        self,
+        *,
+        cache_key: str | None,
+        memory: MemoryState | None,
+        text_tokens: list[list[int]],
+        skip_text_tokens: bool,
+        data_batch: Mapping[str, Any],
+        net: torch.nn.Module | None,
+    ) -> None:
+        if cache_key is None or not isinstance(memory, DiffusionKVCacheMemoryState) or not memory.is_gen_only():
+            return
+        self._remember_instruction_memory(cache_key, memory, data_batch)
+
+        cache_dir_raw = data_batch.get("inference_instruction_cache_dir")
+        namespace = str(data_batch.get("inference_instruction_cache_namespace", ""))
+        if cache_dir_raw is None or not namespace:
+            return
+        if torch.distributed.is_initialized() and torch.distributed.get_world_size() > 1:
+            return
+        identity = self._instruction_memory_identity(
+            text_tokens=text_tokens,
+            skip_text_tokens=skip_text_tokens,
+            namespace=namespace,
+            net=net,
+        )
+        if self._instruction_cache_digest(identity) != cache_key:
+            raise RuntimeError("Instruction Reasoner cache key changed during prefill.")
+
+        cache_dir = Path(str(cache_dir_raw)).expanduser() / "reasoner_kv"
+        cache_dir.mkdir(parents=True, exist_ok=True)
+        cache_path = cache_dir / f"{cache_key}.pt"
+        if cache_path.exists():
+            return
+        tmp_path = cache_dir / f".{cache_key}.{os.getpid()}.tmp"
+        torch.save({"identity": identity, "memory": memory.to_cpu_payload()}, tmp_path)
+        os.replace(tmp_path, cache_path)
+        log.info(f"OmniMoTModel: saved instruction Reasoner K/V cache: {cache_path}")
+
+    def _diffusion_kv_cache_enabled(self) -> bool:
+        return _env_enabled("COSMOS3_REASONER_KVCACHE") or _env_enabled("COSMOS3_REASONER_OFFLOAD")
+
+    def _reasoner_offload_enabled(self) -> bool:
+        return _env_enabled("COSMOS3_REASONER_OFFLOAD")
+
+    def _place_initial_offload_towers(
+        self,
+        net: torch.nn.Module,
+        *,
+        allocate_empty: bool = False,
+        log_result: bool = True,
+    ) -> None:
+        """Materialize the inference tower split before checkpoint loading.
+
+        Generic layerwise offload keeps the full transformer stack on CPU. In
+        reasoner-offload mode, GEN is instead resident on CUDA from model
+        construction onward while only the UND layer path is streamed. DCP
+        loading then writes directly into this mixed-device destination.
+
+        When ``allocate_empty`` is true, tensors that belong on the inference
+        device receive fresh empty storage instead of copying their current
+        contents. This mode is used immediately after ``to_empty(cpu)`` and
+        before ``init_weights`` so uninitialized GEN pages are never read or
+        committed in host RAM.
+        """
+
+        try:
+            layers = net.get_submodule("language_model.model.layers")
+        except AttributeError:
+            return
+
+        cuda_device = torch.device(DEVICE)
+        layer_parameters = {id(parameter) for parameter in layers.parameters()}
+        layer_buffers = {id(buffer) for buffer in layers.buffers()}
+        reasoner_offload = self._reasoner_offload_enabled()
+        generator_resident_cuda = 0
+        generator_total = 0
+
+        def _move_tensor(tensor: torch.Tensor) -> None:
+            if tensor.device.type == cuda_device.type and (
+                cuda_device.index is None or tensor.device.index == cuda_device.index
+            ):
+                return
+            if allocate_empty:
+                tensor.data = torch.empty_like(tensor, device=cuda_device)
+            else:
+                tensor.data = tensor.data.to(cuda_device, non_blocking=True)
+
+        for name, parameter in net.named_parameters():
+            is_generator = self._reasoner_generator_parameter_target(name) == "cuda"
+            should_reside = id(parameter) not in layer_parameters or (reasoner_offload and is_generator)
+            if should_reside:
+                _move_tensor(parameter)
+            if is_generator:
+                size = parameter.numel() * parameter.element_size()
+                generator_total += size
+                if parameter.device.type == "cuda":
+                    generator_resident_cuda += size
+
+        for name, buffer in net.named_buffers():
+            is_generator = self._reasoner_generator_parameter_target(name) == "cuda"
+            should_reside = id(buffer) not in layer_buffers or (reasoner_offload and is_generator)
+            if should_reside:
+                _move_tensor(buffer)
+            if is_generator:
+                size = buffer.numel() * buffer.element_size()
+                generator_total += size
+                if buffer.device.type == "cuda":
+                    generator_resident_cuda += size
+
+        if log_result and cuda_device.type == "cuda":
+            torch.cuda.synchronize()
+        if log_result:
+            log.info(
+                "OmniMoTModel: initial offload placement complete; "
+                f"generator_resident_cuda={generator_resident_cuda / (1024**3):.2f}/"
+                f"{generator_total / (1024**3):.2f} GiB "
+                "cold_materialization=direct_mixed_device"
+            )
+
+    @staticmethod
+    def _reasoner_generator_parameter_target(name: str) -> str | None:
+        """Classify language-model tensors by UND/GEN ownership.
+
+        ``cpu`` identifies UND-owned tensors, but only UND tensors inside a
+        decoder layer are actually offloaded.  Non-layer UND bridge modules
+        stay CUDA-resident across chunks to avoid redundant transfers.
+        """
+
+        layer_prefix = "language_model.model.layers."
+        if name.startswith(layer_prefix):
+            return "cuda" if "_moe_gen" in name else "cpu"
+        reasoner_prefixes = (
+            "language_model.model.embed_tokens.",
+            "language_model.model.norm.",
+            "language_model.lm_head.",
+            "language_model.visual.",
+        )
+        if name.startswith(reasoner_prefixes):
+            return "cpu"
+        return None
+
+    def _offload_reasoner_and_pin_generator(self, net: torch.nn.Module | None = None) -> None:
+        """Keep GEN and reasoner bridge modules resident; offload UND layer tensors."""
+
+        if self._reasoner_offload_complete:
+            return
+        if torch.distributed.is_initialized() and torch.distributed.get_world_size() != 1:
+            raise NotImplementedError("COSMOS3_REASONER_OFFLOAD currently supports single-rank inference only.")
+
+        target = net or self.net
+        cuda_device = torch.device(DEVICE)
+        cpu_device = torch.device("cpu")
+        moved_to_cpu = 0
+        moved_to_cuda = 0
+
+        def _move_tensor(tensor: torch.Tensor, device: torch.device) -> int:
+            same_device = tensor.device.type == device.type and (
+                device.index is None or tensor.device.index == device.index
+            )
+            if same_device:
+                return 0
+            size = tensor.numel() * tensor.element_size()
+            tensor.data = tensor.data.to(device=device, non_blocking=True)
+            return size
+
+        # UND layer tensors should already be back on CPU after streamed
+        # prefill.  Keep the 2.32 GiB non-layer reasoner bridge (embeddings,
+        # visual encoder, final norm/head) on CUDA: every action chunk needs
+        # it for prefill, and evicting/restoring it adds host pressure and a
+        # redundant D2H/H2D cycle without reducing prefill's CUDA peak.
+        layer_prefix = "language_model.model.layers."
+        for name, parameter in target.named_parameters():
+            if name.startswith(layer_prefix) and self._reasoner_generator_parameter_target(name) == "cpu":
+                moved_to_cpu += _move_tensor(parameter, cpu_device)
+        for name, buffer in target.named_buffers():
+            if name.startswith(layer_prefix) and self._reasoner_generator_parameter_target(name) == "cpu":
+                moved_to_cpu += _move_tensor(buffer, cpu_device)
+        torch.cuda.synchronize()
+        if _env_enabled("COSMOS3_REASONER_EMPTY_CACHE"):
+            torch.cuda.empty_cache()
+
+        for name, parameter in target.named_parameters():
+            if self._reasoner_generator_parameter_target(name) == "cuda":
+                moved_to_cuda += _move_tensor(parameter, cuda_device)
+        for name, buffer in target.named_buffers():
+            if self._reasoner_generator_parameter_target(name) == "cuda":
+                moved_to_cuda += _move_tensor(buffer, cuda_device)
+        torch.cuda.synchronize()
+        self._reasoner_offload_complete = True
+        generator_resident_cuda = 0
+        for name, parameter in target.named_parameters():
+            if self._reasoner_generator_parameter_target(name) == "cuda" and parameter.device.type == "cuda":
+                generator_resident_cuda += parameter.numel() * parameter.element_size()
+        for name, buffer in target.named_buffers():
+            if self._reasoner_generator_parameter_target(name) == "cuda" and buffer.device.type == "cuda":
+                generator_resident_cuda += buffer.numel() * buffer.element_size()
+        allocated_gib = torch.cuda.memory_allocated() / (1024**3)
+        reserved_gib = torch.cuda.memory_reserved() / (1024**3)
+        log.info(
+            "OmniMoTModel: UND layer offload finalized after prefill; "
+            f"moved_to_cpu={moved_to_cpu / (1024**3):.2f} GiB "
+            f"generator_to_cuda={moved_to_cuda / (1024**3):.2f} GiB "
+            f"generator_resident_cuda={generator_resident_cuda / (1024**3):.2f} GiB "
+            f"cuda_allocated={allocated_gib:.2f} GiB cuda_reserved={reserved_gib:.2f} GiB"
+        )
+
+    def _prepare_reasoner_prefill(self, net: torch.nn.Module | None = None) -> None:
+        """Prepare a new chunk while leaving an already-resident generator on CUDA."""
+
+        if not self._reasoner_offload_enabled():
+            return
+        if torch.distributed.is_initialized() and torch.distributed.get_world_size() != 1:
+            raise NotImplementedError("COSMOS3_REASONER_OFFLOAD currently supports single-rank inference only.")
+        target = net or self.net
+        cuda_device = torch.device(DEVICE)
+        reasoner_bridge_total = 0
+        reasoner_bridge_cuda = 0
+        layer_prefix = "language_model.model.layers."
+        for name, parameter in target.named_parameters():
+            if self._reasoner_generator_parameter_target(name) == "cpu" and not name.startswith(layer_prefix):
+                size = parameter.numel() * parameter.element_size()
+                reasoner_bridge_total += size
+                if parameter.device.type == cuda_device.type:
+                    reasoner_bridge_cuda += size
+        for name, buffer in target.named_buffers():
+            if self._reasoner_generator_parameter_target(name) == "cpu" and not name.startswith(layer_prefix):
+                size = buffer.numel() * buffer.element_size()
+                reasoner_bridge_total += size
+                if buffer.device.type == cuda_device.type:
+                    reasoner_bridge_cuda += size
+        if reasoner_bridge_cuda != reasoner_bridge_total:
+            raise RuntimeError(
+                "Reasoner bridge modules must remain CUDA-resident between action chunks: "
+                f"{reasoner_bridge_cuda / (1024**3):.2f}/{reasoner_bridge_total / (1024**3):.2f} GiB on CUDA"
+            )
+
+        generator_total = 0
+        generator_remained_cuda = 0
+        for name, parameter in target.named_parameters():
+            if self._reasoner_generator_parameter_target(name) == "cuda":
+                size = parameter.numel() * parameter.element_size()
+                generator_total += size
+                if parameter.device.type == "cuda":
+                    generator_remained_cuda += size
+        for name, buffer in target.named_buffers():
+            if self._reasoner_generator_parameter_target(name) == "cuda":
+                size = buffer.numel() * buffer.element_size()
+                generator_total += size
+                if buffer.device.type == "cuda":
+                    generator_remained_cuda += size
+
+        self._reasoner_offload_complete = False
+        log.info(
+            "OmniMoTModel: prepared reasoner prefill for new chunk; "
+            f"generator_remained_cuda={generator_remained_cuda / (1024**3):.2f}/"
+            f"{generator_total / (1024**3):.2f} GiB "
+            f"reasoner_bridge_remained_cuda={reasoner_bridge_cuda / (1024**3):.2f}/"
+            f"{reasoner_bridge_total / (1024**3):.2f} GiB"
+        )
+
+    def _maybe_finalize_reasoner_offload(
+        self,
+        *,
+        net: torch.nn.Module | None,
+        memories: list[MemoryState | None],
+    ) -> None:
+        if not self._reasoner_offload_enabled() or self._reasoner_offload_complete:
+            return
+        if not memories or any(memory is None or not memory.is_gen_only() for memory in memories):
+            return
+        self._offload_reasoner_and_pin_generator(net)
+
+    def _num_decoder_layers(self, net: torch.nn.Module | None = None) -> int:
+        target = net or self.net
+        layers = target.get_submodule("language_model.model.layers")
+        return len(layers)
+
     def pre_noise_memory_hook(
         self,
         packed_sequence: PackedSequence,
@@ -1588,23 +2007,72 @@ class OmniMoTModel(ImaginaireModel):
         :meth:`tokenize_text`) so there is a single source of truth for
         how raw captions become token ids.
         """
-        use_system_prompt = self.vlm_config.use_system_prompt
+        use_system_prompt = bool(self.vlm_config.use_system_prompt)
         system_prompt: str | None = data_batch.get("system_prompt")
-
-        cond_tokens = self._tokenize_captions(
-            data_batch[self.input_caption_key],
-            use_system_prompt=use_system_prompt,
-            system_prompt=system_prompt,
-            is_video=False,
-        )
+        cond_captions = [str(caption) for caption in data_batch[self.input_caption_key]]
 
         if has_negative_prompt:
             neg_key = "neg_" + self.input_caption_key
             if neg_key not in data_batch:
                 raise ValueError(f"Negative prompt ({neg_key}) not found")
-            uncond_captions = data_batch[neg_key]
+            uncond_captions = [str(caption) for caption in data_batch[neg_key]]
         else:
-            uncond_captions = [""] * len(cond_tokens)
+            uncond_captions = [""] * len(cond_captions)
+
+        tokenizer_identity = {
+            "class": type(self.vlm_tokenizer).__qualname__,
+            "name_or_path": str(getattr(self.vlm_tokenizer, "name_or_path", "")),
+            "vocab_size": int(getattr(self.vlm_tokenizer, "vocab_size", -1)),
+            "special_tokens_map": {
+                str(key): str(value) for key, value in getattr(self.vlm_tokenizer, "special_tokens_map", {}).items()
+            },
+        }
+        identity = {
+            "schema_version": 1,
+            "namespace": str(data_batch.get("inference_instruction_cache_namespace", "")),
+            "tokenizer": tokenizer_identity,
+            "use_system_prompt": use_system_prompt,
+            "system_prompt": system_prompt,
+            "is_video": False,
+            "conditional_captions": cond_captions,
+            "unconditional_captions": uncond_captions,
+        }
+        cache_key = self._instruction_cache_digest(identity)
+        cached = self._inference_text_token_cache.get(cache_key)
+        if cached is not None:
+            return cached
+
+        cache_dir_raw = data_batch.get("inference_instruction_cache_dir")
+        cache_path = (
+            Path(str(cache_dir_raw)).expanduser() / "text_tokens" / f"{cache_key}.json"
+            if cache_dir_raw is not None
+            else None
+        )
+        if cache_path is not None and cache_path.is_file():
+            try:
+                payload = json.loads(cache_path.read_text(encoding="utf-8"))
+                cond_tokens = payload["conditional_tokens"]
+                uncond_tokens = payload["unconditional_tokens"]
+                if payload.get("identity") != identity:
+                    raise ValueError("cache identity does not match the active tokenizer/prompt")
+                if not all(
+                    isinstance(row, list) and all(isinstance(token, int) for token in row)
+                    for row in [*cond_tokens, *uncond_tokens]
+                ):
+                    raise ValueError("cached token IDs must be lists of integers")
+                result = (cond_tokens, uncond_tokens)
+                self._inference_text_token_cache[cache_key] = result
+                log.info(f"OmniMoTModel: instruction text-token disk cache hit: {cache_path}")
+                return result
+            except Exception as exc:
+                log.warning(f"Ignoring invalid instruction text-token cache {cache_path}: {exc}")
+
+        cond_tokens = self._tokenize_captions(
+            cond_captions,
+            use_system_prompt=use_system_prompt,
+            system_prompt=system_prompt,
+            is_video=False,
+        )
 
         uncond_tokens = self._tokenize_captions(
             uncond_captions,
@@ -1612,7 +2080,26 @@ class OmniMoTModel(ImaginaireModel):
             system_prompt=system_prompt,
             is_video=False,
         )
-        return cond_tokens, uncond_tokens
+        result = (cond_tokens, uncond_tokens)
+        self._inference_text_token_cache[cache_key] = result
+        if cache_path is not None:
+            cache_path.parent.mkdir(parents=True, exist_ok=True)
+            tmp_path = cache_path.parent / f".{cache_key}.{os.getpid()}.tmp"
+            tmp_path.write_text(
+                json.dumps(
+                    {
+                        "identity": identity,
+                        "conditional_tokens": cond_tokens,
+                        "unconditional_tokens": uncond_tokens,
+                    },
+                    ensure_ascii=False,
+                    indent=2,
+                ),
+                encoding="utf-8",
+            )
+            os.replace(tmp_path, cache_path)
+            log.info(f"OmniMoTModel: saved instruction text-token cache: {cache_path}")
+        return result
 
     def _prepare_inference_data(
         self,
@@ -1874,6 +2361,8 @@ class OmniMoTModel(ImaginaireModel):
         sequence_plans: list[SequencePlan],
         gen_data_clean: GenerationDataClean,
         skip_text_tokens: bool = False,
+        memory: MemoryState | None = None,
+        und_only: bool = False,
     ) -> list[torch.Tensor]:
         """
         Compute velocity prediction for a single sampling step.
@@ -1894,6 +2383,7 @@ class OmniMoTModel(ImaginaireModel):
             sequence_plans: Pre-computed sequence plans (from _prepare_inference_data)
             gen_data_clean: Pre-computed clean data (from _prepare_inference_data)
             skip_text_tokens: If True, skip text tokens (for CFG unconditional branch)
+            und_only: Populate the understanding K/V cache without computing velocity.
 
         Returns:
             Stacked flattened velocity tensors (one per sample), each containing
@@ -1997,7 +2487,13 @@ class OmniMoTModel(ImaginaireModel):
         out = self.denoise(
             net=net,
             data_batch_packed=packed_sequence,
+            memory=memory,
+            und_only=und_only,
         )
+        if und_only:
+            if memory is None or not memory.is_gen_only():
+                raise RuntimeError("und_only prefill did not populate every diffusion cache layer.")
+            return []
 
         # --- Apply velocity masks ---
         # Zero out velocity for conditioned parts (they don't change during sampling)
@@ -2420,6 +2916,31 @@ class OmniMoTModel(ImaginaireModel):
             _dp_shard_group = None
             _align_device = None
 
+        cond_memory, cond_instruction_cache_key = self._get_instruction_memory_state(
+            text_tokens=cond_tokens,
+            skip_text_tokens=False,
+            data_batch=data_batch,
+            net=net,
+        )
+        if guidance != 1.0:
+            uncond_memory, uncond_instruction_cache_key = self._get_instruction_memory_state(
+                text_tokens=uncond_tokens,
+                skip_text_tokens=skip_text_tokens_for_cfg,
+                data_batch=data_batch,
+                net=net,
+            )
+        else:
+            uncond_memory, uncond_instruction_cache_key = None, None
+
+        # Restore/stream the reasoner only when at least one prompt branch was
+        # not found in the cross-request or disk cache. A full cache hit goes
+        # directly to the generation pathway.
+        needs_reasoner_prefill = (cond_memory is not None and not cond_memory.is_gen_only()) or (
+            guidance != 1.0 and uncond_memory is not None and not uncond_memory.is_gen_only()
+        )
+        if needs_reasoner_prefill:
+            self._prepare_reasoner_prefill(net)
+
         def velocity_fn(noise_x: list[torch.Tensor], timestep: torch.Tensor) -> list[torch.Tensor]:
             # len(noise_x) == B, noise_x[i] is shape (D)
             # timestep is shape (B, 1)
@@ -2431,7 +2952,12 @@ class OmniMoTModel(ImaginaireModel):
             # Expand timestep to (B, 1)
             timestep = timestep.repeat(len(noise_x), 1)
 
-            def _single_velocity_fn(tokens: list[list[int]], skip_text_tokens: bool):
+            def _single_velocity_fn(
+                tokens: list[list[int]],
+                skip_text_tokens: bool,
+                memory: MemoryState | None = None,
+                und_only: bool = False,
+            ):
                 return self._get_velocity(
                     net=net,
                     noise_x=noise_x,
@@ -2440,6 +2966,8 @@ class OmniMoTModel(ImaginaireModel):
                     sequence_plans=sequence_plans,
                     gen_data_clean=gen_data_clean,
                     skip_text_tokens=skip_text_tokens,
+                    memory=memory,
+                    und_only=und_only,
                 )
 
             needs_text_cfg = guidance != 1.0
@@ -2448,6 +2976,47 @@ class OmniMoTModel(ImaginaireModel):
                 t_lo, t_hi = guidance_interval
                 needs_text_cfg = t_lo < timestep[0].item() < t_hi
 
+            # Split prefill from denoising exactly as the decoupled Cosmos3
+            # architecture does: UND runs once to populate per-layer K/V, then
+            # step 0 and every later sampler step run GEN-only.  Interval CFG
+            # still prefills the unconditional cache now because it may be
+            # needed by a later timestep after the reasoner has been evicted.
+            prefill_memories: list[MemoryState | None] = []
+            if cond_memory is not None and not cond_memory.is_gen_only():
+                _single_velocity_fn(
+                    cond_tokens,
+                    skip_text_tokens=False,
+                    memory=cond_memory,
+                    und_only=True,
+                )
+                self._persist_instruction_memory(
+                    cache_key=cond_instruction_cache_key,
+                    memory=cond_memory,
+                    text_tokens=cond_tokens,
+                    skip_text_tokens=False,
+                    data_batch=data_batch,
+                    net=net,
+                )
+                prefill_memories.append(cond_memory)
+            if guidance != 1.0 and uncond_memory is not None and not uncond_memory.is_gen_only():
+                _single_velocity_fn(
+                    uncond_tokens,
+                    skip_text_tokens=skip_text_tokens_for_cfg,
+                    memory=uncond_memory,
+                    und_only=True,
+                )
+                self._persist_instruction_memory(
+                    cache_key=uncond_instruction_cache_key,
+                    memory=uncond_memory,
+                    text_tokens=uncond_tokens,
+                    skip_text_tokens=skip_text_tokens_for_cfg,
+                    data_batch=data_batch,
+                    net=net,
+                )
+                prefill_memories.append(uncond_memory)
+            if prefill_memories:
+                self._maybe_finalize_reasoner_offload(net=net, memories=prefill_memories)
+
             # FSDP alignment: if ANY rank in the shard group needs text-CFG
             # this call, every rank must take the CFG path so the allgather
             # sequence stays aligned across ranks.
@@ -2458,9 +3027,23 @@ class OmniMoTModel(ImaginaireModel):
             else:
                 _any_needs_text_cfg = needs_text_cfg
 
+            def _finish_velocity(
+                result: list[torch.Tensor],
+                *,
+                used_uncond: bool,
+            ) -> list[torch.Tensor]:
+                memories = [cond_memory]
+                if used_uncond:
+                    memories.append(uncond_memory)
+                self._maybe_finalize_reasoner_offload(net=net, memories=memories)
+                return result
+
             # Fast path: no text-CFG anywhere and no postprocess hook — single forward.
             if not _any_needs_text_cfg and velocity_postprocess is None:
-                return _single_velocity_fn(cond_tokens, skip_text_tokens=False)
+                return _finish_velocity(
+                    _single_velocity_fn(cond_tokens, skip_text_tokens=False, memory=cond_memory),
+                    used_uncond=False,
+                )
 
             # Fast path: only text-CFG and no postprocess — preserve the
             # cfgp-parallel branch so two-rank CFG parallelism stays available.
@@ -2469,29 +3052,37 @@ class OmniMoTModel(ImaginaireModel):
                     cond_tokens=cond_tokens,
                     uncond_tokens=uncond_tokens,
                     skip_text_tokens_for_cfg=skip_text_tokens_for_cfg,
-                    single_velocity_fn=_single_velocity_fn,
+                    single_velocity_fn=lambda tokens, skip_text_tokens: _single_velocity_fn(
+                        tokens,
+                        skip_text_tokens,
+                        memory=cond_memory if tokens is cond_tokens else uncond_memory,
+                    ),
                 )
                 if not needs_text_cfg:
                     # Peers needed CFG so we ran the uncond forward to keep
                     # FSDP allgather aligned; locally we still return cond.
-                    return cond_v
+                    return _finish_velocity(cond_v, used_uncond=True)
                 v_pred = [u_i + guidance * (c_i - u_i) for c_i, u_i in zip(cond_v, uncond_v)]
                 if normalize_cfg:
                     v_pred = [
                         v_i * (torch.norm(c_i) / (torch.norm(v_i) + 1e-8)).clamp(min=0.0, max=1.0)
                         for v_i, c_i in zip(v_pred, cond_v)
                     ]
-                return v_pred
+                return _finish_velocity(v_pred, used_uncond=True)
 
             # Conditional forward, then per-step postprocess hook. Hook runs
             # sequentially; cfgp parallelism not used on this path.
-            cond_v_full = _single_velocity_fn(cond_tokens, skip_text_tokens=False)
+            cond_v_full = _single_velocity_fn(cond_tokens, skip_text_tokens=False, memory=cond_memory)
             cond_v = velocity_postprocess(cond_v_full, noise_x, timestep)
 
-            uncond_v = _single_velocity_fn(uncond_tokens, skip_text_tokens=skip_text_tokens_for_cfg)
+            uncond_v = _single_velocity_fn(
+                uncond_tokens,
+                skip_text_tokens=skip_text_tokens_for_cfg,
+                memory=uncond_memory,
+            )
             if not needs_text_cfg:
                 # Same alignment story as above for the postprocess branch.
-                return cond_v
+                return _finish_velocity(cond_v, used_uncond=True)
 
             v_pred = [u_i + guidance * (c_i - u_i) for c_i, u_i in zip(cond_v, uncond_v)]
 
@@ -2501,7 +3092,7 @@ class OmniMoTModel(ImaginaireModel):
                     for v_i, c_i in zip(v_pred, cond_v)
                 ]
 
-            return v_pred
+            return _finish_velocity(v_pred, used_uncond=True)
 
         # Run sampler for all samples at once.
         sampler = sampler or self.sampler
@@ -2902,9 +3493,40 @@ class OmniMoTModel(ImaginaireModel):
         self._normalize_video_databatch_inplace(data_batch)
         self._augment_image_dim_inplace(data_batch)  # converts each image tensor to (1, C, 1, H, W)
         raw_state_vision = data_batch[self.input_image_key if is_image_batch else self.input_video_key]
-        x0_tokens_vision = [
-            self.encode(raw_state_vision_i).contiguous().float() for raw_state_vision_i in raw_state_vision
-        ]
+        condition_only_vae = data_batch.get("inference_condition_only_vae", False) is True
+        sequence_plans = data_batch.get("sequence_plan")
+        condition_only_vae_eligible = (
+            condition_only_vae
+            and not is_image_batch
+            and num_vision_items_per_sample is None
+            and isinstance(sequence_plans, list)
+            and len(sequence_plans) == len(raw_state_vision)
+            and all(getattr(plan, "condition_frame_indexes_vision", None) == [0] for plan in sequence_plans)
+        )
+        if condition_only_vae_eligible:
+            x0_tokens_vision = encode_first_condition_frame(
+                raw_state_vision,
+                tokenizer=self.tokenizer_vision_gen,
+                encode=self.encode,
+            )
+            if not getattr(self, "_condition_only_vae_logged", False):
+                pixel_frames = [int(item.shape[2]) for item in raw_state_vision]
+                latent_frames = [int(item.shape[2]) for item in x0_tokens_vision]
+                log.info(
+                    "OmniMoTModel: condition-only VAE fast path enabled; "
+                    f"encoded_pixel_frames={[1] * len(pixel_frames)} full_pixel_frames={pixel_frames} "
+                    f"latent_frames={latent_frames}"
+                )
+                self._condition_only_vae_logged = True
+        else:
+            if condition_only_vae:
+                log.warning(
+                    "OmniMoTModel: condition-only VAE fast path is not eligible for this batch; "
+                    "falling back to full vision encoding."
+                )
+            x0_tokens_vision = [
+                self.encode(raw_state_vision_i).contiguous().float() for raw_state_vision_i in raw_state_vision
+            ]
 
         frame_size = data_batch.get("image_size", None)
         if frame_size is not None:
@@ -3663,6 +4285,7 @@ class OmniMoTModel(ImaginaireModel):
         net: torch.nn.Module | None = None,
         data_batch_packed: PackedSequence | None = None,
         memory: MemoryState | None = None,
+        und_only: bool = False,
     ) -> dict:
         """
         Runs the MoT network on a packed multi-modal sequence to predict velocity (v) targets.
@@ -3671,6 +4294,7 @@ class OmniMoTModel(ImaginaireModel):
             data_batch_packed: PackedSequence from `pack_input_sequence(...)`.
             memory: Optional pre-built MemoryState for autoregressive generation
                 or KV-cache training.
+            und_only: Populate reasoner K/V without decoding generation outputs.
 
         Returns:
             dict containing:
@@ -3684,7 +4308,10 @@ class OmniMoTModel(ImaginaireModel):
         out_net = net(
             packed_seq=data_batch_packed,
             memory=memory,
+            und_only=und_only,
         )
+        if und_only:
+            return out_net
         output_dict = dict()
         output_dict["preds_vision"] = out_net["preds_vision"]
         if self.config.action_gen and "preds_action" in out_net:
@@ -4597,3 +5224,25 @@ def _is_reasoner_state_dict_key(key: str) -> bool:
     if not key.startswith("language_model."):
         return False
     return "_moe_gen" not in key
+
+
+def _env_enabled(name: str) -> bool:
+    value = os.environ.get(name, "").strip().lower()
+    return value not in ("", "0", "false", "no", "off")
+
+
+class OmniMoTCausalModel(OmniMoTModel):
+    """OmniMoT inference variant with diffusion-step und K/V reuse."""
+
+    def install_attention_dispatch(self, net: torch.nn.Module) -> None:
+        self._install_attention_dispatch_with_memory(net)
+
+    def build_memory_state(
+        self,
+        packed_seq: PackedSequence,
+        memory_info: dict,
+    ) -> MemoryState | None:
+        del packed_seq
+        if not memory_info.get("diffusion_kv_cache", False):
+            return None
+        return DiffusionKVCacheMemoryState(num_layers=self._num_decoder_layers())
diff --git a/cosmos_framework/model/generator/tokenizers/wan2pt2_vae_4x16x16.py b/cosmos_framework/model/generator/tokenizers/wan2pt2_vae_4x16x16.py
index fea6320..344c0eb 100644
--- a/cosmos_framework/model/generator/tokenizers/wan2pt2_vae_4x16x16.py
+++ b/cosmos_framework/model/generator/tokenizers/wan2pt2_vae_4x16x16.py
@@ -11,12 +11,12 @@ import torch.nn as nn
 import torch.nn.functional as F
 from einops import rearrange
 
-from cosmos_framework.utils.flags import DEVICE, INTERNAL, TRAINING
+from cosmos_framework.data.generator.utils import VIDEO_RES_SIZE_INFO
+from cosmos_framework.model.generator.tokenizers.interface import VideoTokenizerInterface
 from cosmos_framework.utils import log
 from cosmos_framework.utils.distributed import get_rank, sync_model_states
 from cosmos_framework.utils.easy_io import easy_io
-from cosmos_framework.data.generator.utils import VIDEO_RES_SIZE_INFO
-from cosmos_framework.model.generator.tokenizers.interface import VideoTokenizerInterface
+from cosmos_framework.utils.flags import DEVICE, INTERNAL, TRAINING
 from cosmos_framework.utils.generator.data_utils import get_vision_data_resolution
 
 # For sequential decoding, CACHE_T is the number of frames to cache.
@@ -1026,7 +1026,11 @@ def _video_vae(
     for b in model.buffers():
         if not b.is_contiguous():
             b.data = b.data.contiguous()
-    sync_model_states(model)
+    # sync_model_states routes through the (NCCL) process group, which only
+    # verifies CUDA tensors. Skip for the CPU-offload build (VAE on CPU);
+    # single-rank has nothing to sync anyway. Multi-rank GPU builds unchanged.
+    if next(model.parameters()).device.type != "cpu":
+        sync_model_states(model)
 
     return model
 
@@ -1045,6 +1049,21 @@ class WanVAE:
     ):
         self.dtype = dtype
         self.device = device
+        # ---- VAE placement anchors (4090 24GB) ----
+        # COSMOS3_VAE_CPU_OFFLOAD keeps the VAE on CPU and swaps it around
+        # encode/decode. COSMOS3_VAE_GPU_RESIDENT explicitly overrides that
+        # path: build/load the VAE directly on GPU and keep it resident.
+        gpu_resident = os.environ.get("COSMOS3_VAE_GPU_RESIDENT", "").strip().lower() in {
+            "1",
+            "true",
+            "yes",
+            "on",
+        }
+        self._vae_offload = bool(os.environ.get("COSMOS3_VAE_CPU_OFFLOAD", "")) and not gpu_resident
+        self._vae_gpu = torch.device(DEVICE)  # device the DiT runs on; VAE swaps onto it
+        build_device = "cpu" if self._vae_offload else device
+        if gpu_resident:
+            log.info("WanVAE: COSMOS3_VAE_GPU_RESIDENT enabled; building VAE directly on GPU")
 
         # Wan 2.2 mean and std values (48 dimensions)
         mean = [
@@ -1148,20 +1167,21 @@ class WanVAE:
             0.7744,
         ]
 
-        mean = torch.tensor(mean, dtype=dtype, device=device)  # [z_dim]
-        std = torch.tensor(std, dtype=dtype, device=device)  # [z_dim]
+        mean = torch.tensor(mean, dtype=dtype, device=build_device)  # [z_dim]
+        std = torch.tensor(std, dtype=dtype, device=build_device)  # [z_dim]
         self.scale = (mean, 1.0 / std)
 
         # init model
         self.model = _video_vae(
             pretrained_path=vae_pth,
             object_store_credential_path_pretrained=object_store_credential_path_pretrained,
-            device=device,
+            device=build_device,
             temporal_window=temporal_window,
             encode_exact_durations=encode_exact_durations,
         )
 
         self.model = self.model.eval().requires_grad_(False)
+        self._vae_cur = torch.device(build_device)
         self.is_amp = is_amp
         if not is_amp:
             self.model = self.model.to(dtype=dtype)
@@ -1172,6 +1192,19 @@ class WanVAE:
     def count_param(self) -> int:
         return sum(p.numel() for p in self.model.parameters())
 
+    def _vae_to(self, device) -> None:
+        """Move VAE weights + scale buffers to ``device``; no-op if already there.
+
+        Used only when VAE CPU offload is enabled (see ``__init__``). Synchronous;
+        the async two-stream variant is a follow-up (see docs/offload).
+        """
+        device = torch.device(device)
+        if device == getattr(self, "_vae_cur", device):
+            return
+        self.model = self.model.to(device)
+        self.scale = tuple(s.to(device) for s in self.scale)
+        self._vae_cur = device
+
     @torch.no_grad()
     def encode(self, videos: torch.Tensor) -> torch.Tensor:
         """Encode a batch of videos.
@@ -1187,12 +1220,16 @@ class WanVAE:
         Returns:
             Tensor of shape ``[B, z_dim, T//4, H//16, W//16]``.
         """
+        if self._vae_offload:
+            self._vae_to(videos.device)  # pull VAE onto the input device (GPU) for encode
         in_dtype = videos.dtype
         with self.context:
             if not self.is_amp:
                 videos = videos.to(self.dtype)
             latent = self.model.encode(videos, self.scale)
         latent = latent.to(in_dtype)
+        if self._vae_offload:
+            self._vae_to("cpu")  # evict VAE → CPU, free GPU for the DiT denoise loop
         return latent
 
     @torch.no_grad()
@@ -1206,12 +1243,16 @@ class WanVAE:
         Returns:
             Tensor of shape ``[B, C, T, H, W]``.
         """
+        if self._vae_offload:
+            self._vae_to(zs.device)  # pull VAE onto the input device (GPU) for decode
         in_dtype = zs.dtype
         with self.context:
             if not self.is_amp:
                 zs = zs.to(self.dtype)
             video_recon = self.model.decode(zs, self.scale, clear_decoder_cache)
         video_recon = video_recon.to(in_dtype)
+        if self._vae_offload:
+            self._vae_to("cpu")  # evict VAE → CPU
         return video_recon
 
 
diff --git a/cosmos_framework/model/generator/utils/condition_only_vae.py b/cosmos_framework/model/generator/utils/condition_only_vae.py
new file mode 100644
index 0000000..693b459
--- /dev/null
+++ b/cosmos_framework/model/generator/utils/condition_only_vae.py
@@ -0,0 +1,65 @@
+# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
+# SPDX-License-Identifier: OpenMDW-1.1
+
+"""Inference helpers for encoding only clean vision condition frames."""
+
+from collections.abc import Callable, Sequence
+from typing import Protocol
+
+import torch
+
+
+class _VisionTokenizer(Protocol):
+    def get_latent_num_frames(self, num_pixel_frames: int) -> int: ...
+
+
+def encode_first_condition_frame(
+    raw_state_vision: Sequence[torch.Tensor],
+    *,
+    tokenizer: _VisionTokenizer,
+    encode: Callable[[torch.Tensor], torch.Tensor],
+) -> list[torch.Tensor]:
+    """Encode frame zero and allocate zero placeholders for generated frames.
+
+    Policy/image-to-video inference conditions only latent frame zero. Clean VAE
+    values for every later frame are multiplied by a zero condition mask before
+    sampling, so encoding those pixel frames is unnecessary. The zero placeholders
+    preserve the full latent shape used by sequence packing and noise generation.
+    """
+
+    latents: list[torch.Tensor] = []
+    for index, raw_state in enumerate(raw_state_vision):
+        if raw_state.ndim != 5:
+            raise ValueError(
+                "Condition-only VAE expects vision tensors shaped [B,C,T,H,W], "
+                f"got item {index} with shape {tuple(raw_state.shape)}."
+            )
+        num_pixel_frames = int(raw_state.shape[2])
+        if num_pixel_frames < 1:
+            raise ValueError(f"Condition-only VAE requires at least one pixel frame, got {num_pixel_frames}.")
+
+        num_latent_frames = int(tokenizer.get_latent_num_frames(num_pixel_frames))
+        if num_latent_frames < 1:
+            raise ValueError(
+                f"Vision tokenizer returned invalid latent frame count {num_latent_frames} "
+                f"for {num_pixel_frames} pixel frames."
+            )
+
+        first_latent = encode(raw_state[:, :, :1]).contiguous().float()
+        if first_latent.ndim != 5 or int(first_latent.shape[2]) != 1:
+            raise ValueError(
+                "Encoding one condition frame must produce one latent frame, "
+                f"got item {index} with shape {tuple(first_latent.shape)}."
+            )
+
+        if num_latent_frames == 1:
+            latents.append(first_latent)
+            continue
+
+        full_shape = list(first_latent.shape)
+        full_shape[2] = num_latent_frames
+        full_latent = first_latent.new_zeros(full_shape)
+        full_latent[:, :, :1].copy_(first_latent)
+        latents.append(full_latent)
+
+    return latents
diff --git a/cosmos_framework/model/generator/utils/condition_only_vae_test.py b/cosmos_framework/model/generator/utils/condition_only_vae_test.py
new file mode 100644
index 0000000..1a90ca2
--- /dev/null
+++ b/cosmos_framework/model/generator/utils/condition_only_vae_test.py
@@ -0,0 +1,39 @@
+# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
+# SPDX-License-Identifier: OpenMDW-1.1
+
+import pytest
+import torch
+
+from cosmos_framework.model.generator.utils.condition_only_vae import encode_first_condition_frame
+
+
+class _Tokenizer:
+    def get_latent_num_frames(self, num_pixel_frames: int) -> int:
+        return 1 + (num_pixel_frames - 1) // 4
+
+
+def test_encode_first_condition_frame_preserves_full_latent_shape() -> None:
+    raw_state = torch.arange(1 * 3 * 33 * 4 * 4, dtype=torch.float32).reshape(1, 3, 33, 4, 4)
+    encoded_temporal_lengths: list[int] = []
+
+    def encode(value: torch.Tensor) -> torch.Tensor:
+        encoded_temporal_lengths.append(int(value.shape[2]))
+        return value[:, :2, :, :2, :2] + 7
+
+    (latent,) = encode_first_condition_frame([raw_state], tokenizer=_Tokenizer(), encode=encode)
+
+    assert encoded_temporal_lengths == [1]
+    assert latent.shape == (1, 2, 9, 2, 2)
+    torch.testing.assert_close(latent[:, :, :1], raw_state[:, :2, :1, :2, :2] + 7)
+    assert torch.count_nonzero(latent[:, :, 1:]) == 0
+
+
+def test_encode_first_condition_frame_rejects_non_single_frame_latent() -> None:
+    raw_state = torch.zeros((1, 3, 33, 4, 4))
+
+    with pytest.raises(ValueError, match="must produce one latent frame"):
+        encode_first_condition_frame(
+            [raw_state],
+            tokenizer=_Tokenizer(),
+            encode=lambda value: value.repeat(1, 1, 2, 1, 1),
+        )
diff --git a/cosmos_framework/model/generator/utils/memory.py b/cosmos_framework/model/generator/utils/memory.py
index 115a8d3..3027267 100644
--- a/cosmos_framework/model/generator/utils/memory.py
+++ b/cosmos_framework/model/generator/utils/memory.py
@@ -19,6 +19,7 @@ the caller can write them back into the cache outside the compile boundary.
 
 from __future__ import annotations
 
+import os
 from abc import ABC, abstractmethod
 from dataclasses import dataclass
 
@@ -49,6 +50,16 @@ class MemoryValue(ABC):
         return True
 
 
+@dataclass
+class DiffusionKVCacheMemoryValue(MemoryValue):
+    """Read-only und K/V snapshot for diffusion gen-only denoising."""
+
+    und_k: torch.Tensor | None = None
+    und_v: torch.Tensor | None = None
+    frame_idx: int = 0
+    for_cuda_graphs: bool = False
+
+
 class MemoryState(ABC):
     """Mutable persistent memory that lives outside ``torch.compile``.
 
@@ -100,3 +111,126 @@ class MemoryState(ABC):
         visibility itself return ``False``.
         """
         return True
+
+
+class DiffusionKVCacheMemoryState(MemoryState):
+    """Cache understanding-tower K/V once, then reuse it for gen-only denoising.
+
+    A separate und-only prefill records the per-layer understanding K/V
+    tensors. Every denoise step, including step 0, then returns cached K/V and
+    marks the decoder as gen-only.
+    """
+
+    def __init__(self, num_layers: int, *, detach: bool = True) -> None:
+        self.num_layers = num_layers
+        self.detach = detach
+        self._und_k: list[torch.Tensor | None] = [None] * num_layers
+        self._und_v: list[torch.Tensor | None] = [None] * num_layers
+        self._initialized = False
+        self._device: torch.device | None = None
+        self._debug_und_input: torch.Tensor | None = None
+
+    def init(self, hidden_states: dict, device: torch.device) -> None:
+        debug_validate_und = os.environ.get("COSMOS3_REASONER_KVCACHE_DEBUG", "").strip().lower() in {
+            "1",
+            "true",
+            "yes",
+            "on",
+        }
+        reasoner_offloaded = os.environ.get("COSMOS3_REASONER_OFFLOAD", "").strip().lower() in {
+            "1",
+            "true",
+            "yes",
+            "on",
+        }
+        if debug_validate_und and not (reasoner_offloaded and self.is_gen_only()):
+            und_len = int(hidden_states["_num_causal_tokens"])
+            und_input = hidden_states["causal_seq"][:und_len].detach()
+            if self._debug_und_input is None:
+                self._debug_und_input = und_input.clone()
+            else:
+                max_abs = (und_input - self._debug_und_input).abs().max().item()
+                if max_abs != 0.0:
+                    raise RuntimeError(
+                        "Diffusion KV-cache invariant failed: decoder understanding input changed "
+                        f"between denoise calls (max_abs={max_abs:.8g})."
+                    )
+        self._initialized = True
+        self._device = device
+
+    def read_for_layer(self, layer_idx: int) -> DiffusionKVCacheMemoryValue:
+        self._check_layer_idx(layer_idx)
+        return DiffusionKVCacheMemoryValue(
+            und_k=self._und_k[layer_idx],
+            und_v=self._und_v[layer_idx],
+            frame_idx=1 if self.is_gen_only() else 0,
+        )
+
+    def write_for_layer(self, layer_idx: int, kv_to_store: KVToStore) -> None:
+        self._check_layer_idx(layer_idx)
+        _gen_k, _gen_v, und_k, und_v = kv_to_store
+        if self.detach:
+            und_k = und_k.detach()
+            und_v = und_v.detach()
+        self._und_k[layer_idx] = und_k
+        self._und_v[layer_idx] = und_v
+
+    def is_gen_only(self) -> bool:
+        return self._initialized and all(k is not None and v is not None for k, v in zip(self._und_k, self._und_v))
+
+    def reset(self) -> None:
+        self._und_k = [None] * self.num_layers
+        self._und_v = [None] * self.num_layers
+        self._initialized = False
+        self._device = None
+        self._debug_und_input = None
+
+    def to_cpu_payload(self) -> dict:
+        """Return a validated, device-independent representation for disk caching."""
+        if not self.is_gen_only():
+            raise RuntimeError("Cannot serialize an incomplete diffusion K/V cache.")
+        return {
+            "schema_version": 1,
+            "num_layers": self.num_layers,
+            "und_k": [tensor.detach().cpu() for tensor in self._und_k],
+            "und_v": [tensor.detach().cpu() for tensor in self._und_v],
+        }
+
+    @classmethod
+    def from_cpu_payload(
+        cls,
+        payload: dict,
+        *,
+        device: torch.device | str,
+        expected_num_layers: int,
+    ) -> "DiffusionKVCacheMemoryState":
+        """Restore a complete cache produced by :meth:`to_cpu_payload`."""
+        if payload.get("schema_version") != 1:
+            raise ValueError(f"Unsupported diffusion K/V cache schema: {payload.get('schema_version')!r}")
+        num_layers = int(payload.get("num_layers", -1))
+        if num_layers != expected_num_layers:
+            raise ValueError(
+                f"Diffusion K/V cache layer mismatch: cache={num_layers}, expected={expected_num_layers}"
+            )
+        und_k = payload.get("und_k")
+        und_v = payload.get("und_v")
+        if not isinstance(und_k, list) or not isinstance(und_v, list):
+            raise ValueError("Diffusion K/V cache must contain und_k and und_v tensor lists.")
+        if len(und_k) != num_layers or len(und_v) != num_layers:
+            raise ValueError(
+                f"Diffusion K/V cache tensor count mismatch: k={len(und_k)}, v={len(und_v)}, layers={num_layers}"
+            )
+        if not all(isinstance(tensor, torch.Tensor) for tensor in [*und_k, *und_v]):
+            raise ValueError("Diffusion K/V cache contains a non-tensor value.")
+
+        target_device = torch.device(device)
+        state = cls(num_layers=num_layers)
+        state._und_k = [tensor.to(target_device) for tensor in und_k]
+        state._und_v = [tensor.to(target_device) for tensor in und_v]
+        state._initialized = True
+        state._device = target_device
+        return state
+
+    def _check_layer_idx(self, layer_idx: int) -> None:
+        if not 0 <= layer_idx < self.num_layers:
+            raise IndexError(f"layer_idx {layer_idx} out of range for {self.num_layers} layers")
diff --git a/cosmos_framework/model/generator/utils/memory_test.py b/cosmos_framework/model/generator/utils/memory_test.py
new file mode 100644
index 0000000..8187b3b
--- /dev/null
+++ b/cosmos_framework/model/generator/utils/memory_test.py
@@ -0,0 +1,57 @@
+# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
+# SPDX-License-Identifier: OpenMDW-1.1
+
+import pytest
+import torch
+
+from cosmos_framework.model.generator.utils.memory import DiffusionKVCacheMemoryState
+
+
+@pytest.mark.L0
+def test_diffusion_kv_cache_memory_state_prefill_then_gen_only():
+    state = DiffusionKVCacheMemoryState(num_layers=2)
+    state.init({}, torch.device("cpu"))
+
+    assert not state.is_gen_only()
+    assert state.read_for_layer(0).und_k is None
+    assert state.read_for_layer(0).frame_idx == 0
+
+    kv0 = tuple(torch.randn(1, 3, 2, 4, requires_grad=True) for _ in range(4))
+    kv1 = tuple(torch.randn(1, 3, 2, 4, requires_grad=True) for _ in range(4))
+    state.write_for_layer(0, kv0)
+    assert not state.is_gen_only()
+    state.write_for_layer(1, kv1)
+
+    assert state.is_gen_only()
+    value = state.read_for_layer(0)
+    assert value.frame_idx == 1
+    assert value.und_k is not None
+    assert value.und_v is not None
+    assert not value.und_k.requires_grad
+    assert not value.und_v.requires_grad
+
+    state.reset()
+    assert not state.is_gen_only()
+
+
+@pytest.mark.L0
+def test_diffusion_kv_cache_cpu_payload_round_trip():
+    state = DiffusionKVCacheMemoryState(num_layers=2)
+    state.init({}, torch.device("cpu"))
+    expected = []
+    for layer_idx in range(2):
+        kv = tuple(torch.randn(1, 3, 2, 4) for _ in range(4))
+        state.write_for_layer(layer_idx, kv)
+        expected.append((kv[2], kv[3]))
+
+    restored = DiffusionKVCacheMemoryState.from_cpu_payload(
+        state.to_cpu_payload(),
+        device="cpu",
+        expected_num_layers=2,
+    )
+
+    assert restored.is_gen_only()
+    for layer_idx, (expected_k, expected_v) in enumerate(expected):
+        value = restored.read_for_layer(layer_idx)
+        torch.testing.assert_close(value.und_k, expected_k)
+        torch.testing.assert_close(value.und_v, expected_v)
