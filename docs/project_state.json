{
  "current_milestone": "M3_baseline",
  "completed_tasks": [
    "M1: metadata infrastructure and reports",
    "M2: HDF5 reader, PiperDualDataset, visualization CLI, split CLI, and dataset smoke tests",
    "M3: created docs/superpowers/plans/2026-06-26-m3-baseline-policy.md",
    "M3: created piper_cosmos/models/baseline_policy.py",
    "M3: created training/train_baseline_piper14.py",
    "M3: created configs/train/baseline_piper14.yaml",
    "M3: created piper_cosmos/eval/offline_action_eval.py",
    "M3: added tests/test_m3_baseline.py",
    "M3: verified train/eval --help",
    "M3: verified py_compile for baseline model, train script, and eval script",
    "M3: wrote skipped smoke report because torch is not available in the current Python environment"
  ],
  "remaining_tasks": [
    "Run 1-2 batch forward/backward smoke in a PyTorch environment, preferably via SLURM if GPU is needed",
    "Record passed forward/backward loss metrics once torch is available",
    "Only after M3 smoke passes, evaluate whether M4 Safety Infrastructure should start"
  ],
  "blockers": [
    "Current Python environment has no torch module; local forward/backward smoke cannot run"
  ],
  "next_task": "Run M3 baseline smoke in a PyTorch environment using training/train_baseline_piper14.py --config configs/train/baseline_piper14.yaml --max-steps 1 --limit-samples 2 --smoke-report reports/baseline_piper14_smoke.json. Do not start M4 until this is verified.",
  "metadata": {
    "fps": 30,
    "raw_hdf5_fps_attr": "absent",
    "gripper_unit": "width",
    "gripper_command_min": 0.0,
    "gripper_command_max": 0.1,
    "action_qpos_alignment": {
      "split": "perfect",
      "num_files": 71,
      "num_steps": 65917,
      "best_alignment": "next",
      "next_global_mean_abs_diff": 0.0009006630583410013,
      "bad_files": 0,
      "report": "reports/action_qpos_alignment_perfect.json"
    },
    "dataset": {
      "data_root": "/project/peilab/wam/physical_WM/data/pack_3_objects_plus/perfect",
      "history_frames": 2,
      "action_horizon": 16,
      "image_size": 224,
      "stride": 1,
      "num_samples": 64781,
      "split_report": "reports/pack_3_objects_plus_split.json"
    },
    "baseline": {
      "model": "SimpleMultiViewCNNPolicy",
      "input_cameras": [
        "cam_high",
        "cam_left_wrist",
        "cam_right_wrist"
      ],
      "qpos_dim": 14,
      "action_horizon": 16,
      "action_dim": 14,
      "loss": "action_mse + 2.0 * gripper_mse + 0.1 * smoothness_loss",
      "config": "configs/train/baseline_piper14.yaml",
      "train_script": "training/train_baseline_piper14.py",
      "eval_script": "piper_cosmos/eval/offline_action_eval.py",
      "smoke_report": "reports/baseline_piper14_smoke.json",
      "eval_report": "reports/baseline_piper14_eval.json",
      "local_smoke_status": "skipped",
      "local_smoke_skip_reason": "torch_not_available"
    }
  },
  "caveats": [
    "M3 did not run forward/backward locally because torch is unavailable.",
    "CUDA training is guarded to require a SLURM environment.",
    "M3 does not include Cosmos3 adapter, Safety Filter, deployment code, or real-robot code."
  ]
}
